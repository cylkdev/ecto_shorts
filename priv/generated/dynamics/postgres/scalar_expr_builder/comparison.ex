defmodule EctoShorts.Dynamics.Postgres.ScalarExpr.Compiled.Comparison do
  @moduledoc false
  import Ecto.Query, only: [dynamic: 2]

  @type selected_binding :: {:as | :at, term()}
  @type key :: atom()
  @type value :: term()
  @type negated :: :not | nil
  @type compose_res :: Ecto.Query.t() | nil

  @doc false
  def dynamic_expr({:as, binding_alias}, key, negated, value) do
    term =
      case negated do
        :not -> {:not, value}
        _ -> value
      end

    case term do
      {:not, {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))
        end

      {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > field(q, ^field_name) + ^scalar_value)
        end

      {:not, {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))
        end

      {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > field(q, ^field_name) - ^scalar_value)
        end

      {:not, {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))
        end

      {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > field(q, ^field_name) * ^scalar_value)
        end

      {:not, {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))
        end

      {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > field(q, ^field_name) / ^scalar_value)
        end

      {:not, {:>, {:value, wrapped_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ^wrapped_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ^wrapped_value))
        end

      {:>, {:value, wrapped_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ^wrapped_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ^wrapped_value)
        end

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")
          )
        end

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "year")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "year")))
        end

      {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "year"))
        end

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "year")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "year")))
        end

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "year"))
        end

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
          )
        end

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
          )
        end

      {:>, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
          )
        end

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
          )
        end

      {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
          )
        end

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")
          )
        end

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "month")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "month")))
        end

      {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "month"))
        end

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "month")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "month")))
        end

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "month"))
        end

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
          )
        end

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
          )
        end

      {:>, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
          )
        end

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
          )
        end

      {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
          )
        end

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")
          )
        end

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "week")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "week")))
        end

      {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "week"))
        end

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "week")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "week")))
        end

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "week"))
        end

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
          )
        end

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
          )
        end

      {:>, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
          )
        end

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
          )
        end

      {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
          )
        end

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")
          )
        end

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "day")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "day")))
        end

      {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "day"))
        end

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "day")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "day")))
        end

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "day"))
        end

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
          )
        end

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
          )
        end

      {:>, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
          )
        end

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
          )
        end

      {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
          )
        end

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")
          )
        end

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "hour")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "hour")))
        end

      {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "hour"))
        end

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "hour")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "hour")))
        end

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "hour"))
        end

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
          )
        end

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
          )
        end

      {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
          )
        end

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
          )
        end

      {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        end

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")
          )
        end

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "minute")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "minute")))
        end

      {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "minute"))
        end

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "minute")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "minute")))
        end

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "minute"))
        end

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
          )
        end

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
          )
        end

      {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
          )
        end

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", from_now(^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", from_now(^datetime_count, "minute")))
          )
        end

      {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        end

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")
          )
        end

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "second")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "second")))
        end

      {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "second"))
        end

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "second")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "second")))
        end

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "second"))
        end

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
          )
        end

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
          )
        end

      {:>, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
          )
        end

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", from_now(^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", from_now(^datetime_count, "second")))
          )
        end

      {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
          )
        end

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
          )
        end

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "millisecond")))
        end

      {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "millisecond"))
        end

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))
        end

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "millisecond"))
        end

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
          )
        end

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", ago(^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", ago(^datetime_count, "millisecond")))
          )
        end

      {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        end

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", from_now(^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", from_now(^datetime_count, "millisecond")))
          )
        end

      {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        end

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
          )
        end

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "microsecond")))
        end

      {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "microsecond"))
        end

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))
        end

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "microsecond"))
        end

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
          )
        end

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", ago(^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", ago(^datetime_count, "microsecond")))
          )
        end

      {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        end

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", from_now(^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", from_now(^datetime_count, "microsecond")))
          )
        end

      {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        end

      {:not, {:>, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ^value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ^value))
        end

      {:>, value} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ^value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ^value)
        end

      {:not, {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))
        end

      {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)
        end

      {:not, {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))
        end

      {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)
        end

      {:not, {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))
        end

      {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)
        end

      {:not, {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))
        end

      {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)
        end

      {:not, {:>=, {:value, wrapped_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ^wrapped_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ^wrapped_value))
        end

      {:>=, {:value, wrapped_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ^wrapped_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ^wrapped_value)
        end

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")
          )
        end

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "year")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "year")))
        end

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "year"))
        end

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "year")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "year")))
        end

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "year"))
        end

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
          )
        end

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
          )
        end

      {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
          )
        end

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
          )
        end

      {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
          )
        end

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")
          )
        end

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "month")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "month")))
        end

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "month"))
        end

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "month")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "month")))
        end

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "month"))
        end

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
          )
        end

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
          )
        end

      {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
          )
        end

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "month")))
          )
        end

      {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
          )
        end

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")
          )
        end

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "week")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "week")))
        end

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "week"))
        end

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "week")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "week")))
        end

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "week"))
        end

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
          )
        end

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
          )
        end

      {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
          )
        end

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
          )
        end

      {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
          )
        end

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")
          )
        end

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "day")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "day")))
        end

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "day"))
        end

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "day")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "day")))
        end

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "day"))
        end

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
          )
        end

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
          )
        end

      {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
          )
        end

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
          )
        end

      {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
          )
        end

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
          )
        end

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "hour")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "hour")))
        end

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "hour"))
        end

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "hour")))
        end

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "hour"))
        end

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
          )
        end

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
          )
        end

      {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
          )
        end

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
          )
        end

      {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        end

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
          )
        end

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "minute")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "minute")))
        end

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "minute"))
        end

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "minute")))
        end

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "minute"))
        end

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
          )
        end

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
          )
        end

      {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
          )
        end

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "minute")))
          )
        end

      {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        end

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")
          )
        end

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "second")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "second")))
        end

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "second"))
        end

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "second")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "second")))
        end

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "second"))
        end

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
          )
        end

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
          )
        end

      {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
          )
        end

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "second")))
          )
        end

      {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
          )
        end

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
          )
        end

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))
        end

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "millisecond"))
        end

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))
        end

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "millisecond"))
        end

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
          )
        end

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", ago(^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", ago(^datetime_count, "millisecond")))
          )
        end

      {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        end

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "millisecond")))
          )
        end

      {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        end

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
          )
        end

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))
        end

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "microsecond"))
        end

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))
        end

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "microsecond"))
        end

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
          )
        end

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", ago(^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", ago(^datetime_count, "microsecond")))
          )
        end

      {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        end

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "microsecond")))
          )
        end

      {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        end

      {:not, {:>=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ^value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ^value))
        end

      {:>=, value} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ^value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ^value)
        end

      {:not, {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))
        end

      {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < field(q, ^field_name) + ^scalar_value)
        end

      {:not, {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))
        end

      {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < field(q, ^field_name) - ^scalar_value)
        end

      {:not, {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))
        end

      {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < field(q, ^field_name) * ^scalar_value)
        end

      {:not, {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))
        end

      {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < field(q, ^field_name) / ^scalar_value)
        end

      {:not, {:<, {:value, wrapped_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ^wrapped_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ^wrapped_value))
        end

      {:<, {:value, wrapped_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ^wrapped_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ^wrapped_value)
        end

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")
          )
        end

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "year")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "year")))
        end

      {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "year"))
        end

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "year")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "year")))
        end

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "year"))
        end

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
          )
        end

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
          )
        end

      {:<, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
          )
        end

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
          )
        end

      {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
          )
        end

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")
          )
        end

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "month")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "month")))
        end

      {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "month"))
        end

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "month")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "month")))
        end

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "month"))
        end

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
          )
        end

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
          )
        end

      {:<, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
          )
        end

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
          )
        end

      {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
          )
        end

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")
          )
        end

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "week")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "week")))
        end

      {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "week"))
        end

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "week")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "week")))
        end

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "week"))
        end

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
          )
        end

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
          )
        end

      {:<, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
          )
        end

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
          )
        end

      {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
          )
        end

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")
          )
        end

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "day")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "day")))
        end

      {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "day"))
        end

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "day")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "day")))
        end

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "day"))
        end

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
          )
        end

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
          )
        end

      {:<, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
          )
        end

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
          )
        end

      {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
          )
        end

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")
          )
        end

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "hour")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "hour")))
        end

      {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "hour"))
        end

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "hour")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "hour")))
        end

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "hour"))
        end

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
          )
        end

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
          )
        end

      {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
          )
        end

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
          )
        end

      {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        end

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")
          )
        end

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "minute")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "minute")))
        end

      {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "minute"))
        end

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "minute")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "minute")))
        end

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "minute"))
        end

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
          )
        end

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
          )
        end

      {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
          )
        end

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", from_now(^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", from_now(^datetime_count, "minute")))
          )
        end

      {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        end

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")
          )
        end

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "second")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "second")))
        end

      {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "second"))
        end

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "second")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "second")))
        end

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "second"))
        end

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
          )
        end

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
          )
        end

      {:<, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
          )
        end

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", from_now(^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", from_now(^datetime_count, "second")))
          )
        end

      {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
          )
        end

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
          )
        end

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "millisecond")))
        end

      {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "millisecond"))
        end

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))
        end

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "millisecond"))
        end

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
          )
        end

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", ago(^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", ago(^datetime_count, "millisecond")))
          )
        end

      {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        end

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", from_now(^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", from_now(^datetime_count, "millisecond")))
          )
        end

      {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        end

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
          )
        end

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "microsecond")))
        end

      {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "microsecond"))
        end

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))
        end

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "microsecond"))
        end

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
          )
        end

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", ago(^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", ago(^datetime_count, "microsecond")))
          )
        end

      {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        end

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", from_now(^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", from_now(^datetime_count, "microsecond")))
          )
        end

      {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        end

      {:not, {:<, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ^value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ^value))
        end

      {:<, value} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ^value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ^value)
        end

      {:not, {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))
        end

      {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)
        end

      {:not, {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))
        end

      {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)
        end

      {:not, {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))
        end

      {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)
        end

      {:not, {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))
        end

      {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)
        end

      {:not, {:<=, {:value, wrapped_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ^wrapped_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ^wrapped_value))
        end

      {:<=, {:value, wrapped_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ^wrapped_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ^wrapped_value)
        end

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")
          )
        end

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "year")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "year")))
        end

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "year"))
        end

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "year")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "year")))
        end

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "year"))
        end

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
          )
        end

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
          )
        end

      {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
          )
        end

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
          )
        end

      {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
          )
        end

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")
          )
        end

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "month")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "month")))
        end

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "month"))
        end

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "month")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "month")))
        end

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "month"))
        end

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
          )
        end

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
          )
        end

      {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
          )
        end

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "month")))
          )
        end

      {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
          )
        end

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")
          )
        end

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "week")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "week")))
        end

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "week"))
        end

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "week")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "week")))
        end

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "week"))
        end

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
          )
        end

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
          )
        end

      {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
          )
        end

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
          )
        end

      {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
          )
        end

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")
          )
        end

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "day")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "day")))
        end

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "day"))
        end

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "day")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "day")))
        end

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "day"))
        end

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
          )
        end

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
          )
        end

      {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
          )
        end

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
          )
        end

      {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
          )
        end

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
          )
        end

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "hour")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "hour")))
        end

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "hour"))
        end

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "hour")))
        end

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "hour"))
        end

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
          )
        end

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
          )
        end

      {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
          )
        end

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
          )
        end

      {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        end

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
          )
        end

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "minute")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "minute")))
        end

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "minute"))
        end

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "minute")))
        end

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "minute"))
        end

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
          )
        end

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
          )
        end

      {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
          )
        end

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "minute")))
          )
        end

      {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        end

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")
          )
        end

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "second")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "second")))
        end

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "second"))
        end

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "second")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "second")))
        end

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "second"))
        end

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
          )
        end

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
          )
        end

      {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
          )
        end

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "second")))
          )
        end

      {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
          )
        end

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
          )
        end

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))
        end

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "millisecond"))
        end

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))
        end

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "millisecond"))
        end

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
          )
        end

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", ago(^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", ago(^datetime_count, "millisecond")))
          )
        end

      {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        end

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "millisecond")))
          )
        end

      {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        end

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
          )
        end

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))
        end

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "microsecond"))
        end

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))
        end

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "microsecond"))
        end

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
          )
        end

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", ago(^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", ago(^datetime_count, "microsecond")))
          )
        end

      {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        end

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "microsecond")))
          )
        end

      {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        end

      {:not, {:<=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ^value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ^value))
        end

      {:<=, value} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ^value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ^value)
        end

      {:not, {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))
        end

      {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > field(q, ^field_name) + ^scalar_value)
        end

      {:not, {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))
        end

      {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > field(q, ^field_name) - ^scalar_value)
        end

      {:not, {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))
        end

      {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > field(q, ^field_name) * ^scalar_value)
        end

      {:not, {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))
        end

      {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > field(q, ^field_name) / ^scalar_value)
        end

      {:not, {:gt, {:value, wrapped_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ^wrapped_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ^wrapped_value))
        end

      {:gt, {:value, wrapped_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ^wrapped_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ^wrapped_value)
        end

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")
          )
        end

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "year")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "year")))
        end

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "year"))
        end

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "year")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "year")))
        end

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "year"))
        end

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
          )
        end

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
          )
        end

      {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
          )
        end

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
          )
        end

      {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
          )
        end

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")
          )
        end

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "month")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "month")))
        end

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "month"))
        end

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "month")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "month")))
        end

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "month"))
        end

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
          )
        end

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
          )
        end

      {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
          )
        end

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
          )
        end

      {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
          )
        end

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")
          )
        end

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "week")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "week")))
        end

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "week"))
        end

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "week")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "week")))
        end

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "week"))
        end

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
          )
        end

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
          )
        end

      {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
          )
        end

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
          )
        end

      {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
          )
        end

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")
          )
        end

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "day")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "day")))
        end

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "day"))
        end

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "day")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "day")))
        end

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "day"))
        end

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
          )
        end

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
          )
        end

      {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
          )
        end

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
          )
        end

      {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
          )
        end

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")
          )
        end

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "hour")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "hour")))
        end

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "hour"))
        end

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "hour")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "hour")))
        end

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "hour"))
        end

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
          )
        end

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
          )
        end

      {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
          )
        end

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
          )
        end

      {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        end

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")
          )
        end

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "minute")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "minute")))
        end

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "minute"))
        end

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "minute")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "minute")))
        end

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "minute"))
        end

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
          )
        end

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
          )
        end

      {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
          )
        end

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", from_now(^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", from_now(^datetime_count, "minute")))
          )
        end

      {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        end

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")
          )
        end

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "second")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "second")))
        end

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "second"))
        end

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "second")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "second")))
        end

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "second"))
        end

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
          )
        end

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
          )
        end

      {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
          )
        end

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", from_now(^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", from_now(^datetime_count, "second")))
          )
        end

      {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
          )
        end

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
          )
        end

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "millisecond")))
        end

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "millisecond"))
        end

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))
        end

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "millisecond"))
        end

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
          )
        end

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", ago(^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", ago(^datetime_count, "millisecond")))
          )
        end

      {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        end

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", from_now(^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", from_now(^datetime_count, "millisecond")))
          )
        end

      {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        end

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
          )
        end

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ago(^datetime_count, "microsecond")))
        end

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ago(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ago(^datetime_count, "microsecond"))
        end

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))
        end

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > from_now(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > from_now(^datetime_count, "microsecond"))
        end

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
          )
        end

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", ago(^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", ago(^datetime_count, "microsecond")))
          )
        end

      {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        end

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", from_now(^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >
                   fragment("date(?)", from_now(^datetime_count, "microsecond")))
          )
        end

      {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        end

      {:not, {:gt, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) > ^value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) > ^value))
        end

      {:gt, value} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) > ^value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) > ^value)
        end

      {:not, {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))
        end

      {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)
        end

      {:not, {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))
        end

      {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)
        end

      {:not, {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))
        end

      {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)
        end

      {:not, {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))
        end

      {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)
        end

      {:not, {:gte, {:value, wrapped_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ^wrapped_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ^wrapped_value))
        end

      {:gte, {:value, wrapped_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ^wrapped_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ^wrapped_value)
        end

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")
          )
        end

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "year")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "year")))
        end

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "year"))
        end

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "year")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "year")))
        end

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "year"))
        end

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
          )
        end

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
          )
        end

      {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
          )
        end

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
          )
        end

      {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
          )
        end

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")
          )
        end

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "month")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "month")))
        end

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "month"))
        end

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "month")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "month")))
        end

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "month"))
        end

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
          )
        end

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
          )
        end

      {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
          )
        end

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "month")))
          )
        end

      {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
          )
        end

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")
          )
        end

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "week")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "week")))
        end

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "week"))
        end

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "week")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "week")))
        end

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "week"))
        end

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
          )
        end

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
          )
        end

      {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
          )
        end

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
          )
        end

      {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
          )
        end

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")
          )
        end

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "day")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "day")))
        end

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "day"))
        end

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "day")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "day")))
        end

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "day"))
        end

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
          )
        end

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
          )
        end

      {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
          )
        end

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
          )
        end

      {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
          )
        end

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
          )
        end

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "hour")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "hour")))
        end

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "hour"))
        end

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "hour")))
        end

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "hour"))
        end

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
          )
        end

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
          )
        end

      {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
          )
        end

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
          )
        end

      {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        end

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
          )
        end

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "minute")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "minute")))
        end

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "minute"))
        end

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "minute")))
        end

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "minute"))
        end

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
          )
        end

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
          )
        end

      {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
          )
        end

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "minute")))
          )
        end

      {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        end

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")
          )
        end

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "second")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "second")))
        end

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "second"))
        end

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "second")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "second")))
        end

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "second"))
        end

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
          )
        end

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
          )
        end

      {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
          )
        end

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "second")))
          )
        end

      {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
          )
        end

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
          )
        end

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))
        end

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "millisecond"))
        end

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))
        end

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "millisecond"))
        end

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
          )
        end

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", ago(^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", ago(^datetime_count, "millisecond")))
          )
        end

      {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        end

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "millisecond")))
          )
        end

      {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        end

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
          )
        end

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))
        end

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ago(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ago(^datetime_count, "microsecond"))
        end

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))
        end

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= from_now(^datetime_count, "microsecond"))
        end

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
          )
        end

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", ago(^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", ago(^datetime_count, "microsecond")))
          )
        end

      {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        end

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) >=
                   fragment("date(?)", from_now(^datetime_count, "microsecond")))
          )
        end

      {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) >=
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        end

      {:not, {:gte, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) >= ^value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) >= ^value))
        end

      {:gte, value} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) >= ^value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) >= ^value)
        end

      {:not, {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))
        end

      {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < field(q, ^field_name) + ^scalar_value)
        end

      {:not, {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))
        end

      {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < field(q, ^field_name) - ^scalar_value)
        end

      {:not, {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))
        end

      {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < field(q, ^field_name) * ^scalar_value)
        end

      {:not, {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))
        end

      {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < field(q, ^field_name) / ^scalar_value)
        end

      {:not, {:lt, {:value, wrapped_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ^wrapped_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ^wrapped_value))
        end

      {:lt, {:value, wrapped_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ^wrapped_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ^wrapped_value)
        end

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")
          )
        end

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "year")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "year")))
        end

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "year"))
        end

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "year")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "year")))
        end

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "year"))
        end

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
          )
        end

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
          )
        end

      {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
          )
        end

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
          )
        end

      {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
          )
        end

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")
          )
        end

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "month")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "month")))
        end

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "month"))
        end

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "month")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "month")))
        end

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "month"))
        end

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
          )
        end

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
          )
        end

      {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
          )
        end

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
          )
        end

      {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
          )
        end

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")
          )
        end

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "week")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "week")))
        end

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "week"))
        end

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "week")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "week")))
        end

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "week"))
        end

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
          )
        end

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
          )
        end

      {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
          )
        end

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
          )
        end

      {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
          )
        end

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")
          )
        end

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "day")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "day")))
        end

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "day"))
        end

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "day")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "day")))
        end

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "day"))
        end

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
          )
        end

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
          )
        end

      {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
          )
        end

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
          )
        end

      {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
          )
        end

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")
          )
        end

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "hour")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "hour")))
        end

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "hour"))
        end

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "hour")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "hour")))
        end

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "hour"))
        end

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
          )
        end

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
          )
        end

      {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
          )
        end

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
          )
        end

      {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        end

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")
          )
        end

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "minute")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "minute")))
        end

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "minute"))
        end

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "minute")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "minute")))
        end

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "minute"))
        end

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
          )
        end

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
          )
        end

      {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
          )
        end

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", from_now(^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", from_now(^datetime_count, "minute")))
          )
        end

      {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        end

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")
          )
        end

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "second")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "second")))
        end

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "second"))
        end

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "second")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "second")))
        end

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "second"))
        end

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
          )
        end

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
          )
        end

      {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
          )
        end

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", from_now(^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", from_now(^datetime_count, "second")))
          )
        end

      {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
          )
        end

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
          )
        end

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "millisecond")))
        end

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "millisecond"))
        end

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))
        end

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "millisecond"))
        end

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
          )
        end

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", ago(^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", ago(^datetime_count, "millisecond")))
          )
        end

      {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        end

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", from_now(^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", from_now(^datetime_count, "millisecond")))
          )
        end

      {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        end

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
          )
        end

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ago(^datetime_count, "microsecond")))
        end

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ago(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ago(^datetime_count, "microsecond"))
        end

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))
        end

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < from_now(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < from_now(^datetime_count, "microsecond"))
        end

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
          )
        end

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", ago(^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", ago(^datetime_count, "microsecond")))
          )
        end

      {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        end

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", from_now(^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <
                   fragment("date(?)", from_now(^datetime_count, "microsecond")))
          )
        end

      {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        end

      {:not, {:lt, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) < ^value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) < ^value))
        end

      {:lt, value} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) < ^value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) < ^value)
        end

      {:not, {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))
        end

      {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)
        end

      {:not, {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))
        end

      {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)
        end

      {:not, {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))
        end

      {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)
        end

      {:not, {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))
        end

      {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)
        end

      {:not, {:lte, {:value, wrapped_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ^wrapped_value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ^wrapped_value))
        end

      {:lte, {:value, wrapped_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ^wrapped_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ^wrapped_value)
        end

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")
          )
        end

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "year")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "year")))
        end

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "year"))
        end

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "year")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "year")))
        end

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "year"))
        end

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
          )
        end

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
          )
        end

      {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
          )
        end

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
          )
        end

      {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
          )
        end

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")
          )
        end

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "month")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "month")))
        end

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "month"))
        end

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "month")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "month")))
        end

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "month"))
        end

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
          )
        end

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
          )
        end

      {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
          )
        end

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "month")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "month")))
          )
        end

      {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
          )
        end

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")
          )
        end

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "week")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "week")))
        end

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "week"))
        end

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "week")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "week")))
        end

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "week"))
        end

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
          )
        end

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
          )
        end

      {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
          )
        end

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
          )
        end

      {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
          )
        end

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")
          )
        end

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "day")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "day")))
        end

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "day"))
        end

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "day")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "day")))
        end

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "day"))
        end

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
          )
        end

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
          )
        end

      {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
          )
        end

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
          )
        end

      {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
          )
        end

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
          )
        end

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "hour")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "hour")))
        end

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "hour"))
        end

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "hour")))
        end

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "hour"))
        end

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
          )
        end

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
          )
        end

      {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
          )
        end

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
          )
        end

      {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        end

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
          )
        end

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "minute")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "minute")))
        end

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "minute"))
        end

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "minute")))
        end

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "minute"))
        end

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
          )
        end

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
          )
        end

      {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
          )
        end

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "minute")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "minute")))
          )
        end

      {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        end

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")
          )
        end

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "second")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "second")))
        end

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "second"))
        end

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "second")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "second")))
        end

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "second"))
        end

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
          )
        end

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
          )
        end

      {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
          )
        end

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "second")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "second")))
          )
        end

      {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
          )
        end

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
          )
        end

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))
        end

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "millisecond"))
        end

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))
        end

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "millisecond"))
        end

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
          )
        end

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", ago(^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", ago(^datetime_count, "millisecond")))
          )
        end

      {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        end

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "millisecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "millisecond")))
          )
        end

      {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        end

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
          )
        end

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))
        end

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ago(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ago(^datetime_count, "microsecond"))
        end

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))
        end

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= from_now(^datetime_count, "microsecond"))
        end

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
          )
        end

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", ago(^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", ago(^datetime_count, "microsecond")))
          )
        end

      {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        end

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "microsecond")))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            not (fragment("date(?)", field(q, ^key)) <=
                   fragment("date(?)", from_now(^datetime_count, "microsecond")))
          )
        end

      {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) <=
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        end

      {:not, {:lte, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) <= ^value))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) <= ^value))
        end

      {:lte, value} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) <= ^value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) <= ^value)
        end

      {:not, {:==, {:all, quantified_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) == all(quantified_value)))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) == all(quantified_value)))
        end

      {:==, {:all, quantified_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == all(quantified_value))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == all(quantified_value))
        end

      {:not, {:==, {:any, quantified_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) == any(quantified_value)))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) == any(quantified_value)))
        end

      {:==, {:any, quantified_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == any(quantified_value))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == any(quantified_value))
        end

      {:not, {:==, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], not is_nil(field(q, ^key)))
        else
          dynamic([{^binding_alias, q}], not is_nil(field(q, ^key)))
        end

      {:==, nil} ->
        if is_nil(binding_alias) do
          dynamic([q], is_nil(field(q, ^key)))
        else
          dynamic([{^binding_alias, q}], is_nil(field(q, ^key)))
        end

      {:not, {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value})
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value}
          )
        end

      {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value})
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value}
          )
        end

      {:not, {:==, {:value, wrapped_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^wrapped_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ^wrapped_value)
        end

      {:==, {:value, wrapped_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^wrapped_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ^wrapped_value)
        end

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
          )
        end

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
          )
        end

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "year"))
        end

      {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "year"))
        end

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "year"))
        end

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "year"))
        end

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
          )
        end

      {:==, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
          )
        end

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
          )
        end

      {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
          )
        end

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
          )
        end

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
          )
        end

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "month"))
        end

      {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "month"))
        end

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "month"))
        end

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "month"))
        end

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
          )
        end

      {:==, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
          )
        end

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
          )
        end

      {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
          )
        end

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
          )
        end

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
          )
        end

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "week"))
        end

      {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "week"))
        end

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "week"))
        end

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "week"))
        end

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
          )
        end

      {:==, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
          )
        end

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
          )
        end

      {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
          )
        end

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
          )
        end

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
          )
        end

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "day"))
        end

      {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "day"))
        end

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "day"))
        end

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "day"))
        end

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
          )
        end

      {:==, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
          )
        end

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
          )
        end

      {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
          )
        end

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
          )
        end

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
          )
        end

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "hour"))
        end

      {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "hour"))
        end

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "hour"))
        end

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "hour"))
        end

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
          )
        end

      {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
          )
        end

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        end

      {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        end

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
          )
        end

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
          )
        end

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "minute"))
        end

      {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "minute"))
        end

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "minute"))
        end

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "minute"))
        end

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
          )
        end

      {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
          )
        end

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        end

      {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        end

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
          )
        end

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
          )
        end

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "second"))
        end

      {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "second"))
        end

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "second"))
        end

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "second"))
        end

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
          )
        end

      {:==, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
          )
        end

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
          )
        end

      {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
          )
        end

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
          )
        end

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
          )
        end

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "millisecond"))
        end

      {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "millisecond"))
        end

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "millisecond"))
        end

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "millisecond"))
        end

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        end

      {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        end

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        end

      {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        end

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
          )
        end

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
          )
        end

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "microsecond"))
        end

      {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "microsecond"))
        end

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "microsecond"))
        end

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "microsecond"))
        end

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        end

      {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        end

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        end

      {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        end

      {:not, {:==, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ^value)
        end

      {:==, value} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ^value)
        end

      {:not, {:eq, {:all, quantified_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) == all(quantified_value)))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) == all(quantified_value)))
        end

      {:eq, {:all, quantified_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == all(quantified_value))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == all(quantified_value))
        end

      {:not, {:eq, {:any, quantified_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) == any(quantified_value)))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) == any(quantified_value)))
        end

      {:eq, {:any, quantified_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == any(quantified_value))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == any(quantified_value))
        end

      {:not, {:eq, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], not is_nil(field(q, ^key)))
        else
          dynamic([{^binding_alias, q}], not is_nil(field(q, ^key)))
        end

      {:eq, nil} ->
        if is_nil(binding_alias) do
          dynamic([q], is_nil(field(q, ^key)))
        else
          dynamic([{^binding_alias, q}], is_nil(field(q, ^key)))
        end

      {:not, {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value})
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value}
          )
        end

      {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value})
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value}
          )
        end

      {:not, {:eq, {:value, wrapped_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^wrapped_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ^wrapped_value)
        end

      {:eq, {:value, wrapped_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^wrapped_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ^wrapped_value)
        end

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
          )
        end

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
          )
        end

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "year"))
        end

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "year"))
        end

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "year"))
        end

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "year"))
        end

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
          )
        end

      {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
          )
        end

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
          )
        end

      {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
          )
        end

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
          )
        end

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
          )
        end

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "month"))
        end

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "month"))
        end

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "month"))
        end

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "month"))
        end

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
          )
        end

      {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
          )
        end

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
          )
        end

      {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
          )
        end

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
          )
        end

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
          )
        end

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "week"))
        end

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "week"))
        end

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "week"))
        end

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "week"))
        end

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
          )
        end

      {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
          )
        end

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
          )
        end

      {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
          )
        end

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
          )
        end

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
          )
        end

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "day"))
        end

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "day"))
        end

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "day"))
        end

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "day"))
        end

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
          )
        end

      {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
          )
        end

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
          )
        end

      {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
          )
        end

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
          )
        end

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
          )
        end

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "hour"))
        end

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "hour"))
        end

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "hour"))
        end

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "hour"))
        end

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
          )
        end

      {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
          )
        end

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        end

      {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        end

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
          )
        end

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
          )
        end

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "minute"))
        end

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "minute"))
        end

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "minute"))
        end

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "minute"))
        end

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
          )
        end

      {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
          )
        end

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        end

      {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        end

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
          )
        end

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
          )
        end

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "second"))
        end

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "second"))
        end

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "second"))
        end

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "second"))
        end

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
          )
        end

      {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
          )
        end

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
          )
        end

      {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
          )
        end

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
          )
        end

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
          )
        end

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "millisecond"))
        end

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "millisecond"))
        end

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "millisecond"))
        end

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "millisecond"))
        end

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        end

      {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        end

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        end

      {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        end

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
          )
        end

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
          )
        end

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "microsecond"))
        end

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "microsecond"))
        end

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "microsecond"))
        end

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "microsecond"))
        end

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        end

      {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        end

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        end

      {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        end

      {:not, {:eq, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ^value)
        end

      {:eq, value} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ^value)
        end

      {:not, {:!=, {:all, quantified_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) != all(quantified_value)))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) != all(quantified_value)))
        end

      {:!=, {:all, quantified_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != all(quantified_value))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != all(quantified_value))
        end

      {:not, {:!=, {:any, quantified_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) != any(quantified_value)))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) != any(quantified_value)))
        end

      {:!=, {:any, quantified_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != any(quantified_value))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != any(quantified_value))
        end

      {:not, {:!=, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], is_nil(field(q, ^key)))
        else
          dynamic([{^binding_alias, q}], is_nil(field(q, ^key)))
        end

      {:!=, nil} ->
        if is_nil(binding_alias) do
          dynamic([q], not is_nil(field(q, ^key)))
        else
          dynamic([{^binding_alias, q}], not is_nil(field(q, ^key)))
        end

      {:not, {:!=, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})
        end

      {:!=, {:value, {:+, field: field_name, value: scalar_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})
        end

      {:not, {:!=, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})
        end

      {:!=, {:value, {:-, field: field_name, value: scalar_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})
        end

      {:not, {:!=, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})
        end

      {:!=, {:value, {:*, field: field_name, value: scalar_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})
        end

      {:not, {:!=, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})
        end

      {:!=, {:value, {:/, field: field_name, value: scalar_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})
        end

      {:not, {:!=, {:value, wrapped_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^wrapped_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ^wrapped_value)
        end

      {:!=, {:value, wrapped_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^wrapped_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ^wrapped_value)
        end

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
          )
        end

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
          )
        end

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "year"))
        end

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "year"))
        end

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "year"))
        end

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "year"))
        end

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
          )
        end

      {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
          )
        end

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
          )
        end

      {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
          )
        end

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
          )
        end

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
          )
        end

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "month"))
        end

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "month"))
        end

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "month"))
        end

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "month"))
        end

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
          )
        end

      {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
          )
        end

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
          )
        end

      {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
          )
        end

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
          )
        end

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
          )
        end

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "week"))
        end

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "week"))
        end

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "week"))
        end

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "week"))
        end

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
          )
        end

      {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
          )
        end

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
          )
        end

      {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
          )
        end

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
          )
        end

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
          )
        end

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "day"))
        end

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "day"))
        end

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "day"))
        end

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "day"))
        end

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
          )
        end

      {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
          )
        end

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
          )
        end

      {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
          )
        end

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
          )
        end

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
          )
        end

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "hour"))
        end

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "hour"))
        end

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "hour"))
        end

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "hour"))
        end

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
          )
        end

      {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
          )
        end

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        end

      {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        end

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
          )
        end

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
          )
        end

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "minute"))
        end

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "minute"))
        end

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "minute"))
        end

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "minute"))
        end

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
          )
        end

      {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
          )
        end

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        end

      {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        end

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
          )
        end

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
          )
        end

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "second"))
        end

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "second"))
        end

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "second"))
        end

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "second"))
        end

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
          )
        end

      {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
          )
        end

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
          )
        end

      {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
          )
        end

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
          )
        end

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
          )
        end

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "millisecond"))
        end

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "millisecond"))
        end

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "millisecond"))
        end

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "millisecond"))
        end

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        end

      {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        end

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        end

      {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        end

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
          )
        end

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
          )
        end

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "microsecond"))
        end

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "microsecond"))
        end

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "microsecond"))
        end

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "microsecond"))
        end

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        end

      {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        end

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        end

      {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        end

      {:not, {:!=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ^value)
        end

      {:!=, value} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ^value)
        end

      {:not, {:ne, {:all, quantified_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) != all(quantified_value)))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) != all(quantified_value)))
        end

      {:ne, {:all, quantified_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != all(quantified_value))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != all(quantified_value))
        end

      {:not, {:ne, {:any, quantified_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (field(q, ^key) != any(quantified_value)))
        else
          dynamic([{^binding_alias, q}], not (field(q, ^key) != any(quantified_value)))
        end

      {:ne, {:any, quantified_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != any(quantified_value))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != any(quantified_value))
        end

      {:not, {:ne, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], is_nil(field(q, ^key)))
        else
          dynamic([{^binding_alias, q}], is_nil(field(q, ^key)))
        end

      {:ne, nil} ->
        if is_nil(binding_alias) do
          dynamic([q], not is_nil(field(q, ^key)))
        else
          dynamic([{^binding_alias, q}], not is_nil(field(q, ^key)))
        end

      {:not, {:ne, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})
        end

      {:ne, {:value, {:+, field: field_name, value: scalar_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})
        end

      {:not, {:ne, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})
        end

      {:ne, {:value, {:-, field: field_name, value: scalar_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})
        end

      {:not, {:ne, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})
        end

      {:ne, {:value, {:*, field: field_name, value: scalar_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})
        end

      {:not, {:ne, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})
        end

      {:ne, {:value, {:/, field: field_name, value: scalar_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})
        end

      {:not, {:ne, {:value, wrapped_value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^wrapped_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ^wrapped_value)
        end

      {:ne, {:value, wrapped_value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^wrapped_value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ^wrapped_value)
        end

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
          )
        end

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
          )
        end

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "year"))
        end

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "year"))
        end

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "year"))
        end

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "year"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "year"))
        end

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
          )
        end

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
          )
        end

      {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
          )
        end

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
          )
        end

      {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
          )
        end

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
          )
        end

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
          )
        end

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "month"))
        end

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "month"))
        end

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "month"))
        end

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "month"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "month"))
        end

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
          )
        end

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
          )
        end

      {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
          )
        end

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
          )
        end

      {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
          )
        end

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
          )
        end

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
          )
        end

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "week"))
        end

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "week"))
        end

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "week"))
        end

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "week"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "week"))
        end

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
          )
        end

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
          )
        end

      {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
          )
        end

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
          )
        end

      {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
          )
        end

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
          )
        end

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
          )
        end

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "day"))
        end

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "day"))
        end

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "day"))
        end

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "day"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "day"))
        end

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
          )
        end

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
          )
        end

      {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
          )
        end

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
          )
        end

      {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
          )
        end

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
          )
        end

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
          )
        end

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "hour"))
        end

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "hour"))
        end

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "hour"))
        end

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "hour"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "hour"))
        end

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
          )
        end

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
          )
        end

      {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
          )
        end

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        end

      {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
          )
        end

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
          )
        end

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
          )
        end

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "minute"))
        end

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "minute"))
        end

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "minute"))
        end

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "minute"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "minute"))
        end

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
          )
        end

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
          )
        end

      {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
          )
        end

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        end

      {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
          )
        end

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
          )
        end

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
          )
        end

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "second"))
        end

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "second"))
        end

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "second"))
        end

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "second"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "second"))
        end

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
          )
        end

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
          )
        end

      {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
          )
        end

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
          )
        end

      {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
          )
        end

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
          )
        end

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
          )
        end

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "millisecond"))
        end

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "millisecond"))
        end

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "millisecond"))
        end

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "millisecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "millisecond"))
        end

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
          )
        end

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        end

      {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
          )
        end

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        end

      {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", from_now(^datetime_count, "millisecond"))
          )
        end

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
          )
        end

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        else
          dynamic(
            [{^binding_alias, q}],
            field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
          )
        end

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ago(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ago(^datetime_count, "microsecond"))
        end

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ago(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ago(^datetime_count, "microsecond"))
        end

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == from_now(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == from_now(^datetime_count, "microsecond"))
        end

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != from_now(^datetime_count, "microsecond"))
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != from_now(^datetime_count, "microsecond"))
        end

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
          )
        end

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        end

      {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
          )
        end

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) ==
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        end

      {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        if is_nil(binding_alias) do
          dynamic(
            [q],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        else
          dynamic(
            [{^binding_alias, q}],
            fragment("date(?)", field(q, ^key)) !=
              fragment("date(?)", from_now(^datetime_count, "microsecond"))
          )
        end

      {:not, {:ne, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) == ^value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) == ^value)
        end

      {:ne, value} ->
        if is_nil(binding_alias) do
          dynamic([q], field(q, ^key) != ^value)
        else
          dynamic([{^binding_alias, q}], field(q, ^key) != ^value)
        end

      {:not, {:avg, {:>, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (avg(field(q, ^key)) > ^value))
        else
          dynamic([{^binding_alias, q}], not (avg(field(q, ^key)) > ^value))
        end

      {:avg, {:>, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) > ^value)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) > ^value)
        end

      {:not, {:avg, {:>=, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (avg(field(q, ^key)) >= ^value))
        else
          dynamic([{^binding_alias, q}], not (avg(field(q, ^key)) >= ^value))
        end

      {:avg, {:>=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) >= ^value)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) >= ^value)
        end

      {:not, {:avg, {:<, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (avg(field(q, ^key)) < ^value))
        else
          dynamic([{^binding_alias, q}], not (avg(field(q, ^key)) < ^value))
        end

      {:avg, {:<, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) < ^value)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) < ^value)
        end

      {:not, {:avg, {:<=, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (avg(field(q, ^key)) <= ^value))
        else
          dynamic([{^binding_alias, q}], not (avg(field(q, ^key)) <= ^value))
        end

      {:avg, {:<=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) <= ^value)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) <= ^value)
        end

      {:not, {:avg, {:gt, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (avg(field(q, ^key)) > ^value))
        else
          dynamic([{^binding_alias, q}], not (avg(field(q, ^key)) > ^value))
        end

      {:avg, {:gt, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) > ^value)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) > ^value)
        end

      {:not, {:avg, {:gte, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (avg(field(q, ^key)) >= ^value))
        else
          dynamic([{^binding_alias, q}], not (avg(field(q, ^key)) >= ^value))
        end

      {:avg, {:gte, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) >= ^value)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) >= ^value)
        end

      {:not, {:avg, {:lt, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (avg(field(q, ^key)) < ^value))
        else
          dynamic([{^binding_alias, q}], not (avg(field(q, ^key)) < ^value))
        end

      {:avg, {:lt, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) < ^value)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) < ^value)
        end

      {:not, {:avg, {:lte, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (avg(field(q, ^key)) <= ^value))
        else
          dynamic([{^binding_alias, q}], not (avg(field(q, ^key)) <= ^value))
        end

      {:avg, {:lte, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) <= ^value)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) <= ^value)
        end

      {:not, {:avg, {:==, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) != ^nil)
        end

      {:avg, {:==, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) == ^nil)
        end

      {:not, {:avg, {:==, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) != ^value)
        end

      {:avg, {:==, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) == ^value)
        end

      {:not, {:avg, {:eq, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) != ^nil)
        end

      {:avg, {:eq, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) == ^nil)
        end

      {:not, {:avg, {:eq, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) != ^value)
        end

      {:avg, {:eq, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) == ^value)
        end

      {:not, {:avg, {:!=, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) == ^nil)
        end

      {:avg, {:!=, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) != ^nil)
        end

      {:not, {:avg, {:!=, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) == ^value)
        end

      {:avg, {:!=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) != ^value)
        end

      {:not, {:avg, {:ne, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) == ^nil)
        end

      {:avg, {:ne, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) != ^nil)
        end

      {:not, {:avg, {:ne, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) == ^value)
        end

      {:avg, {:ne, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], avg(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], avg(field(q, ^key)) != ^value)
        end

      {:not, {:count, {:>, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (count(field(q, ^key)) > ^value))
        else
          dynamic([{^binding_alias, q}], not (count(field(q, ^key)) > ^value))
        end

      {:count, {:>, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) > ^value)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) > ^value)
        end

      {:not, {:count, {:>=, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (count(field(q, ^key)) >= ^value))
        else
          dynamic([{^binding_alias, q}], not (count(field(q, ^key)) >= ^value))
        end

      {:count, {:>=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) >= ^value)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) >= ^value)
        end

      {:not, {:count, {:<, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (count(field(q, ^key)) < ^value))
        else
          dynamic([{^binding_alias, q}], not (count(field(q, ^key)) < ^value))
        end

      {:count, {:<, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) < ^value)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) < ^value)
        end

      {:not, {:count, {:<=, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (count(field(q, ^key)) <= ^value))
        else
          dynamic([{^binding_alias, q}], not (count(field(q, ^key)) <= ^value))
        end

      {:count, {:<=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) <= ^value)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) <= ^value)
        end

      {:not, {:count, {:gt, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (count(field(q, ^key)) > ^value))
        else
          dynamic([{^binding_alias, q}], not (count(field(q, ^key)) > ^value))
        end

      {:count, {:gt, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) > ^value)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) > ^value)
        end

      {:not, {:count, {:gte, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (count(field(q, ^key)) >= ^value))
        else
          dynamic([{^binding_alias, q}], not (count(field(q, ^key)) >= ^value))
        end

      {:count, {:gte, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) >= ^value)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) >= ^value)
        end

      {:not, {:count, {:lt, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (count(field(q, ^key)) < ^value))
        else
          dynamic([{^binding_alias, q}], not (count(field(q, ^key)) < ^value))
        end

      {:count, {:lt, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) < ^value)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) < ^value)
        end

      {:not, {:count, {:lte, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (count(field(q, ^key)) <= ^value))
        else
          dynamic([{^binding_alias, q}], not (count(field(q, ^key)) <= ^value))
        end

      {:count, {:lte, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) <= ^value)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) <= ^value)
        end

      {:not, {:count, {:==, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) != ^nil)
        end

      {:count, {:==, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) == ^nil)
        end

      {:not, {:count, {:==, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) != ^value)
        end

      {:count, {:==, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) == ^value)
        end

      {:not, {:count, {:eq, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) != ^nil)
        end

      {:count, {:eq, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) == ^nil)
        end

      {:not, {:count, {:eq, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) != ^value)
        end

      {:count, {:eq, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) == ^value)
        end

      {:not, {:count, {:!=, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) == ^nil)
        end

      {:count, {:!=, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) != ^nil)
        end

      {:not, {:count, {:!=, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) == ^value)
        end

      {:count, {:!=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) != ^value)
        end

      {:not, {:count, {:ne, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) == ^nil)
        end

      {:count, {:ne, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) != ^nil)
        end

      {:not, {:count, {:ne, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) == ^value)
        end

      {:count, {:ne, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], count(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], count(field(q, ^key)) != ^value)
        end

      {:not, {:max, {:>, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (max(field(q, ^key)) > ^value))
        else
          dynamic([{^binding_alias, q}], not (max(field(q, ^key)) > ^value))
        end

      {:max, {:>, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) > ^value)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) > ^value)
        end

      {:not, {:max, {:>=, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (max(field(q, ^key)) >= ^value))
        else
          dynamic([{^binding_alias, q}], not (max(field(q, ^key)) >= ^value))
        end

      {:max, {:>=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) >= ^value)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) >= ^value)
        end

      {:not, {:max, {:<, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (max(field(q, ^key)) < ^value))
        else
          dynamic([{^binding_alias, q}], not (max(field(q, ^key)) < ^value))
        end

      {:max, {:<, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) < ^value)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) < ^value)
        end

      {:not, {:max, {:<=, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (max(field(q, ^key)) <= ^value))
        else
          dynamic([{^binding_alias, q}], not (max(field(q, ^key)) <= ^value))
        end

      {:max, {:<=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) <= ^value)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) <= ^value)
        end

      {:not, {:max, {:gt, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (max(field(q, ^key)) > ^value))
        else
          dynamic([{^binding_alias, q}], not (max(field(q, ^key)) > ^value))
        end

      {:max, {:gt, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) > ^value)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) > ^value)
        end

      {:not, {:max, {:gte, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (max(field(q, ^key)) >= ^value))
        else
          dynamic([{^binding_alias, q}], not (max(field(q, ^key)) >= ^value))
        end

      {:max, {:gte, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) >= ^value)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) >= ^value)
        end

      {:not, {:max, {:lt, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (max(field(q, ^key)) < ^value))
        else
          dynamic([{^binding_alias, q}], not (max(field(q, ^key)) < ^value))
        end

      {:max, {:lt, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) < ^value)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) < ^value)
        end

      {:not, {:max, {:lte, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (max(field(q, ^key)) <= ^value))
        else
          dynamic([{^binding_alias, q}], not (max(field(q, ^key)) <= ^value))
        end

      {:max, {:lte, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) <= ^value)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) <= ^value)
        end

      {:not, {:max, {:==, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) != ^nil)
        end

      {:max, {:==, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) == ^nil)
        end

      {:not, {:max, {:==, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) != ^value)
        end

      {:max, {:==, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) == ^value)
        end

      {:not, {:max, {:eq, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) != ^nil)
        end

      {:max, {:eq, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) == ^nil)
        end

      {:not, {:max, {:eq, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) != ^value)
        end

      {:max, {:eq, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) == ^value)
        end

      {:not, {:max, {:!=, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) == ^nil)
        end

      {:max, {:!=, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) != ^nil)
        end

      {:not, {:max, {:!=, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) == ^value)
        end

      {:max, {:!=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) != ^value)
        end

      {:not, {:max, {:ne, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) == ^nil)
        end

      {:max, {:ne, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) != ^nil)
        end

      {:not, {:max, {:ne, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) == ^value)
        end

      {:max, {:ne, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], max(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], max(field(q, ^key)) != ^value)
        end

      {:not, {:min, {:>, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (min(field(q, ^key)) > ^value))
        else
          dynamic([{^binding_alias, q}], not (min(field(q, ^key)) > ^value))
        end

      {:min, {:>, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) > ^value)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) > ^value)
        end

      {:not, {:min, {:>=, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (min(field(q, ^key)) >= ^value))
        else
          dynamic([{^binding_alias, q}], not (min(field(q, ^key)) >= ^value))
        end

      {:min, {:>=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) >= ^value)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) >= ^value)
        end

      {:not, {:min, {:<, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (min(field(q, ^key)) < ^value))
        else
          dynamic([{^binding_alias, q}], not (min(field(q, ^key)) < ^value))
        end

      {:min, {:<, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) < ^value)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) < ^value)
        end

      {:not, {:min, {:<=, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (min(field(q, ^key)) <= ^value))
        else
          dynamic([{^binding_alias, q}], not (min(field(q, ^key)) <= ^value))
        end

      {:min, {:<=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) <= ^value)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) <= ^value)
        end

      {:not, {:min, {:gt, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (min(field(q, ^key)) > ^value))
        else
          dynamic([{^binding_alias, q}], not (min(field(q, ^key)) > ^value))
        end

      {:min, {:gt, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) > ^value)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) > ^value)
        end

      {:not, {:min, {:gte, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (min(field(q, ^key)) >= ^value))
        else
          dynamic([{^binding_alias, q}], not (min(field(q, ^key)) >= ^value))
        end

      {:min, {:gte, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) >= ^value)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) >= ^value)
        end

      {:not, {:min, {:lt, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (min(field(q, ^key)) < ^value))
        else
          dynamic([{^binding_alias, q}], not (min(field(q, ^key)) < ^value))
        end

      {:min, {:lt, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) < ^value)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) < ^value)
        end

      {:not, {:min, {:lte, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (min(field(q, ^key)) <= ^value))
        else
          dynamic([{^binding_alias, q}], not (min(field(q, ^key)) <= ^value))
        end

      {:min, {:lte, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) <= ^value)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) <= ^value)
        end

      {:not, {:min, {:==, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) != ^nil)
        end

      {:min, {:==, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) == ^nil)
        end

      {:not, {:min, {:==, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) != ^value)
        end

      {:min, {:==, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) == ^value)
        end

      {:not, {:min, {:eq, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) != ^nil)
        end

      {:min, {:eq, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) == ^nil)
        end

      {:not, {:min, {:eq, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) != ^value)
        end

      {:min, {:eq, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) == ^value)
        end

      {:not, {:min, {:!=, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) == ^nil)
        end

      {:min, {:!=, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) != ^nil)
        end

      {:not, {:min, {:!=, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) == ^value)
        end

      {:min, {:!=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) != ^value)
        end

      {:not, {:min, {:ne, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) == ^nil)
        end

      {:min, {:ne, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) != ^nil)
        end

      {:not, {:min, {:ne, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) == ^value)
        end

      {:min, {:ne, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], min(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], min(field(q, ^key)) != ^value)
        end

      {:not, {:sum, {:>, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (sum(field(q, ^key)) > ^value))
        else
          dynamic([{^binding_alias, q}], not (sum(field(q, ^key)) > ^value))
        end

      {:sum, {:>, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) > ^value)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) > ^value)
        end

      {:not, {:sum, {:>=, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (sum(field(q, ^key)) >= ^value))
        else
          dynamic([{^binding_alias, q}], not (sum(field(q, ^key)) >= ^value))
        end

      {:sum, {:>=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) >= ^value)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) >= ^value)
        end

      {:not, {:sum, {:<, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (sum(field(q, ^key)) < ^value))
        else
          dynamic([{^binding_alias, q}], not (sum(field(q, ^key)) < ^value))
        end

      {:sum, {:<, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) < ^value)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) < ^value)
        end

      {:not, {:sum, {:<=, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (sum(field(q, ^key)) <= ^value))
        else
          dynamic([{^binding_alias, q}], not (sum(field(q, ^key)) <= ^value))
        end

      {:sum, {:<=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) <= ^value)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) <= ^value)
        end

      {:not, {:sum, {:gt, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (sum(field(q, ^key)) > ^value))
        else
          dynamic([{^binding_alias, q}], not (sum(field(q, ^key)) > ^value))
        end

      {:sum, {:gt, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) > ^value)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) > ^value)
        end

      {:not, {:sum, {:gte, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (sum(field(q, ^key)) >= ^value))
        else
          dynamic([{^binding_alias, q}], not (sum(field(q, ^key)) >= ^value))
        end

      {:sum, {:gte, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) >= ^value)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) >= ^value)
        end

      {:not, {:sum, {:lt, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (sum(field(q, ^key)) < ^value))
        else
          dynamic([{^binding_alias, q}], not (sum(field(q, ^key)) < ^value))
        end

      {:sum, {:lt, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) < ^value)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) < ^value)
        end

      {:not, {:sum, {:lte, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], not (sum(field(q, ^key)) <= ^value))
        else
          dynamic([{^binding_alias, q}], not (sum(field(q, ^key)) <= ^value))
        end

      {:sum, {:lte, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) <= ^value)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) <= ^value)
        end

      {:not, {:sum, {:==, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) != ^nil)
        end

      {:sum, {:==, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) == ^nil)
        end

      {:not, {:sum, {:==, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) != ^value)
        end

      {:sum, {:==, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) == ^value)
        end

      {:not, {:sum, {:eq, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) != ^nil)
        end

      {:sum, {:eq, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) == ^nil)
        end

      {:not, {:sum, {:eq, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) != ^value)
        end

      {:sum, {:eq, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) == ^value)
        end

      {:not, {:sum, {:!=, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) == ^nil)
        end

      {:sum, {:!=, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) != ^nil)
        end

      {:not, {:sum, {:!=, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) == ^value)
        end

      {:sum, {:!=, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) != ^value)
        end

      {:not, {:sum, {:ne, nil}}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) == ^nil)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) == ^nil)
        end

      {:sum, {:ne, nil}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) != ^nil)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) != ^nil)
        end

      {:not, {:sum, {:ne, value}}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) == ^value)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) == ^value)
        end

      {:sum, {:ne, value}} ->
        if is_nil(binding_alias) do
          dynamic([q], sum(field(q, ^key)) != ^value)
        else
          dynamic([{^binding_alias, q}], sum(field(q, ^key)) != ^value)
        end
    end
  end

  def dynamic_expr({:at, 1}, key, negated, value) do
    term =
      case negated do
        :not -> {:not, value}
        _ -> value
      end

    case term do
      {:not, {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:>, {:value, wrapped_value}}} ->
        dynamic([q], not (field(q, ^key) > ^wrapped_value))

      {:>, {:value, wrapped_value}} ->
        dynamic([q], field(q, ^key) > ^wrapped_value)

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")))

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")))

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")))

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")))

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")))

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")))

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>, value}} ->
        dynamic([q], not (field(q, ^key) > ^value))

      {:>, value} ->
        dynamic([q], field(q, ^key) > ^value)

      {:not, {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:>=, {:value, wrapped_value}}} ->
        dynamic([q], not (field(q, ^key) >= ^wrapped_value))

      {:>=, {:value, wrapped_value}} ->
        dynamic([q], field(q, ^key) >= ^wrapped_value)

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")))

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")))

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")))

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")))

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")))

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")))

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>=, value}} ->
        dynamic([q], not (field(q, ^key) >= ^value))

      {:>=, value} ->
        dynamic([q], field(q, ^key) >= ^value)

      {:not, {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:<, {:value, wrapped_value}}} ->
        dynamic([q], not (field(q, ^key) < ^wrapped_value))

      {:<, {:value, wrapped_value}} ->
        dynamic([q], field(q, ^key) < ^wrapped_value)

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")))

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")))

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")))

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")))

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")))

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")))

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<, value}} ->
        dynamic([q], not (field(q, ^key) < ^value))

      {:<, value} ->
        dynamic([q], field(q, ^key) < ^value)

      {:not, {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:<=, {:value, wrapped_value}}} ->
        dynamic([q], not (field(q, ^key) <= ^wrapped_value))

      {:<=, {:value, wrapped_value}} ->
        dynamic([q], field(q, ^key) <= ^wrapped_value)

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")))

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")))

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")))

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")))

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")))

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")))

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<=, value}} ->
        dynamic([q], not (field(q, ^key) <= ^value))

      {:<=, value} ->
        dynamic([q], field(q, ^key) <= ^value)

      {:not, {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:gt, {:value, wrapped_value}}} ->
        dynamic([q], not (field(q, ^key) > ^wrapped_value))

      {:gt, {:value, wrapped_value}} ->
        dynamic([q], field(q, ^key) > ^wrapped_value)

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")))

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")))

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")))

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")))

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")))

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")))

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gt, value}} ->
        dynamic([q], not (field(q, ^key) > ^value))

      {:gt, value} ->
        dynamic([q], field(q, ^key) > ^value)

      {:not, {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:gte, {:value, wrapped_value}}} ->
        dynamic([q], not (field(q, ^key) >= ^wrapped_value))

      {:gte, {:value, wrapped_value}} ->
        dynamic([q], field(q, ^key) >= ^wrapped_value)

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")))

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")))

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")))

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")))

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")))

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")))

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gte, value}} ->
        dynamic([q], not (field(q, ^key) >= ^value))

      {:gte, value} ->
        dynamic([q], field(q, ^key) >= ^value)

      {:not, {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:lt, {:value, wrapped_value}}} ->
        dynamic([q], not (field(q, ^key) < ^wrapped_value))

      {:lt, {:value, wrapped_value}} ->
        dynamic([q], field(q, ^key) < ^wrapped_value)

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")))

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")))

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")))

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")))

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")))

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")))

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lt, value}} ->
        dynamic([q], not (field(q, ^key) < ^value))

      {:lt, value} ->
        dynamic([q], field(q, ^key) < ^value)

      {:not, {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:lte, {:value, wrapped_value}}} ->
        dynamic([q], not (field(q, ^key) <= ^wrapped_value))

      {:lte, {:value, wrapped_value}} ->
        dynamic([q], field(q, ^key) <= ^wrapped_value)

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")))

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")))

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")))

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")))

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")))

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")))

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lte, value}} ->
        dynamic([q], not (field(q, ^key) <= ^value))

      {:lte, value} ->
        dynamic([q], field(q, ^key) <= ^value)

      {:not, {:==, {:all, quantified_value}}} ->
        dynamic([q], not (field(q, ^key) == all(quantified_value)))

      {:==, {:all, quantified_value}} ->
        dynamic([q], field(q, ^key) == all(quantified_value))

      {:not, {:==, {:any, quantified_value}}} ->
        dynamic([q], not (field(q, ^key) == any(quantified_value)))

      {:==, {:any, quantified_value}} ->
        dynamic([q], field(q, ^key) == any(quantified_value))

      {:not, {:==, nil}} ->
        dynamic([q], not is_nil(field(q, ^key)))

      {:==, nil} ->
        dynamic([q], is_nil(field(q, ^key)))

      {:not, {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([q], field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value})

      {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([q], field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value})

      {:not, {:==, {:value, wrapped_value}}} ->
        dynamic([q], field(q, ^key) != ^wrapped_value)

      {:==, {:value, wrapped_value}} ->
        dynamic([q], field(q, ^key) == ^wrapped_value)

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "year"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year")))

      {:==, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year")))

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "month"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "week"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week")))

      {:==, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week")))

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "day"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day")))

      {:==, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day")))

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour")))

      {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour")))

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "second"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:==, value}} ->
        dynamic([q], field(q, ^key) != ^value)

      {:==, value} ->
        dynamic([q], field(q, ^key) == ^value)

      {:not, {:eq, {:all, quantified_value}}} ->
        dynamic([q], not (field(q, ^key) == all(quantified_value)))

      {:eq, {:all, quantified_value}} ->
        dynamic([q], field(q, ^key) == all(quantified_value))

      {:not, {:eq, {:any, quantified_value}}} ->
        dynamic([q], not (field(q, ^key) == any(quantified_value)))

      {:eq, {:any, quantified_value}} ->
        dynamic([q], field(q, ^key) == any(quantified_value))

      {:not, {:eq, nil}} ->
        dynamic([q], not is_nil(field(q, ^key)))

      {:eq, nil} ->
        dynamic([q], is_nil(field(q, ^key)))

      {:not, {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([q], field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value})

      {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([q], field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value})

      {:not, {:eq, {:value, wrapped_value}}} ->
        dynamic([q], field(q, ^key) != ^wrapped_value)

      {:eq, {:value, wrapped_value}} ->
        dynamic([q], field(q, ^key) == ^wrapped_value)

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "year"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year")))

      {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year")))

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "month"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "week"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week")))

      {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week")))

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "day"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day")))

      {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day")))

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour")))

      {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour")))

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "second"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:eq, value}} ->
        dynamic([q], field(q, ^key) != ^value)

      {:eq, value} ->
        dynamic([q], field(q, ^key) == ^value)

      {:not, {:!=, {:all, quantified_value}}} ->
        dynamic([q], not (field(q, ^key) != all(quantified_value)))

      {:!=, {:all, quantified_value}} ->
        dynamic([q], field(q, ^key) != all(quantified_value))

      {:not, {:!=, {:any, quantified_value}}} ->
        dynamic([q], not (field(q, ^key) != any(quantified_value)))

      {:!=, {:any, quantified_value}} ->
        dynamic([q], field(q, ^key) != any(quantified_value))

      {:not, {:!=, nil}} ->
        dynamic([q], is_nil(field(q, ^key)))

      {:!=, nil} ->
        dynamic([q], not is_nil(field(q, ^key)))

      {:not, {:!=, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:!=, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:!=, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:!=, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:!=, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, wrapped_value}}} ->
        dynamic([q], field(q, ^key) == ^wrapped_value)

      {:!=, {:value, wrapped_value}} ->
        dynamic([q], field(q, ^key) != ^wrapped_value)

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "year"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year")))

      {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year")))

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "month"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "week"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week")))

      {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week")))

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "day"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day")))

      {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day")))

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour")))

      {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour")))

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "second"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:!=, value}} ->
        dynamic([q], field(q, ^key) == ^value)

      {:!=, value} ->
        dynamic([q], field(q, ^key) != ^value)

      {:not, {:ne, {:all, quantified_value}}} ->
        dynamic([q], not (field(q, ^key) != all(quantified_value)))

      {:ne, {:all, quantified_value}} ->
        dynamic([q], field(q, ^key) != all(quantified_value))

      {:not, {:ne, {:any, quantified_value}}} ->
        dynamic([q], not (field(q, ^key) != any(quantified_value)))

      {:ne, {:any, quantified_value}} ->
        dynamic([q], field(q, ^key) != any(quantified_value))

      {:not, {:ne, nil}} ->
        dynamic([q], is_nil(field(q, ^key)))

      {:ne, nil} ->
        dynamic([q], not is_nil(field(q, ^key)))

      {:not, {:ne, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:ne, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:ne, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:ne, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:ne, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, wrapped_value}}} ->
        dynamic([q], field(q, ^key) == ^wrapped_value)

      {:ne, {:value, wrapped_value}} ->
        dynamic([q], field(q, ^key) != ^wrapped_value)

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "year"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year")))

      {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year")))

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "month"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "week"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week")))

      {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week")))

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "day"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day")))

      {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day")))

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour")))

      {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([q], fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour")))

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "second"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:ne, value}} ->
        dynamic([q], field(q, ^key) == ^value)

      {:ne, value} ->
        dynamic([q], field(q, ^key) != ^value)

      {:not, {:avg, {:>, value}}} ->
        dynamic([q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:>, value}} ->
        dynamic([q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:>=, value}}} ->
        dynamic([q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:>=, value}} ->
        dynamic([q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:<, value}}} ->
        dynamic([q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:<, value}} ->
        dynamic([q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:<=, value}}} ->
        dynamic([q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:<=, value}} ->
        dynamic([q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:gt, value}}} ->
        dynamic([q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:gt, value}} ->
        dynamic([q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:gte, value}}} ->
        dynamic([q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:gte, value}} ->
        dynamic([q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:lt, value}}} ->
        dynamic([q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:lt, value}} ->
        dynamic([q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:lte, value}}} ->
        dynamic([q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:lte, value}} ->
        dynamic([q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:==, nil}}} ->
        dynamic([q], avg(field(q, ^key)) != ^nil)

      {:avg, {:==, nil}} ->
        dynamic([q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:==, value}}} ->
        dynamic([q], avg(field(q, ^key)) != ^value)

      {:avg, {:==, value}} ->
        dynamic([q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:eq, nil}}} ->
        dynamic([q], avg(field(q, ^key)) != ^nil)

      {:avg, {:eq, nil}} ->
        dynamic([q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:eq, value}}} ->
        dynamic([q], avg(field(q, ^key)) != ^value)

      {:avg, {:eq, value}} ->
        dynamic([q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:!=, nil}}} ->
        dynamic([q], avg(field(q, ^key)) == ^nil)

      {:avg, {:!=, nil}} ->
        dynamic([q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:!=, value}}} ->
        dynamic([q], avg(field(q, ^key)) == ^value)

      {:avg, {:!=, value}} ->
        dynamic([q], avg(field(q, ^key)) != ^value)

      {:not, {:avg, {:ne, nil}}} ->
        dynamic([q], avg(field(q, ^key)) == ^nil)

      {:avg, {:ne, nil}} ->
        dynamic([q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:ne, value}}} ->
        dynamic([q], avg(field(q, ^key)) == ^value)

      {:avg, {:ne, value}} ->
        dynamic([q], avg(field(q, ^key)) != ^value)

      {:not, {:count, {:>, value}}} ->
        dynamic([q], not (count(field(q, ^key)) > ^value))

      {:count, {:>, value}} ->
        dynamic([q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:>=, value}}} ->
        dynamic([q], not (count(field(q, ^key)) >= ^value))

      {:count, {:>=, value}} ->
        dynamic([q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:<, value}}} ->
        dynamic([q], not (count(field(q, ^key)) < ^value))

      {:count, {:<, value}} ->
        dynamic([q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:<=, value}}} ->
        dynamic([q], not (count(field(q, ^key)) <= ^value))

      {:count, {:<=, value}} ->
        dynamic([q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:gt, value}}} ->
        dynamic([q], not (count(field(q, ^key)) > ^value))

      {:count, {:gt, value}} ->
        dynamic([q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:gte, value}}} ->
        dynamic([q], not (count(field(q, ^key)) >= ^value))

      {:count, {:gte, value}} ->
        dynamic([q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:lt, value}}} ->
        dynamic([q], not (count(field(q, ^key)) < ^value))

      {:count, {:lt, value}} ->
        dynamic([q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:lte, value}}} ->
        dynamic([q], not (count(field(q, ^key)) <= ^value))

      {:count, {:lte, value}} ->
        dynamic([q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:==, nil}}} ->
        dynamic([q], count(field(q, ^key)) != ^nil)

      {:count, {:==, nil}} ->
        dynamic([q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:==, value}}} ->
        dynamic([q], count(field(q, ^key)) != ^value)

      {:count, {:==, value}} ->
        dynamic([q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:eq, nil}}} ->
        dynamic([q], count(field(q, ^key)) != ^nil)

      {:count, {:eq, nil}} ->
        dynamic([q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:eq, value}}} ->
        dynamic([q], count(field(q, ^key)) != ^value)

      {:count, {:eq, value}} ->
        dynamic([q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:!=, nil}}} ->
        dynamic([q], count(field(q, ^key)) == ^nil)

      {:count, {:!=, nil}} ->
        dynamic([q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:!=, value}}} ->
        dynamic([q], count(field(q, ^key)) == ^value)

      {:count, {:!=, value}} ->
        dynamic([q], count(field(q, ^key)) != ^value)

      {:not, {:count, {:ne, nil}}} ->
        dynamic([q], count(field(q, ^key)) == ^nil)

      {:count, {:ne, nil}} ->
        dynamic([q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:ne, value}}} ->
        dynamic([q], count(field(q, ^key)) == ^value)

      {:count, {:ne, value}} ->
        dynamic([q], count(field(q, ^key)) != ^value)

      {:not, {:max, {:>, value}}} ->
        dynamic([q], not (max(field(q, ^key)) > ^value))

      {:max, {:>, value}} ->
        dynamic([q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:>=, value}}} ->
        dynamic([q], not (max(field(q, ^key)) >= ^value))

      {:max, {:>=, value}} ->
        dynamic([q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:<, value}}} ->
        dynamic([q], not (max(field(q, ^key)) < ^value))

      {:max, {:<, value}} ->
        dynamic([q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:<=, value}}} ->
        dynamic([q], not (max(field(q, ^key)) <= ^value))

      {:max, {:<=, value}} ->
        dynamic([q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:gt, value}}} ->
        dynamic([q], not (max(field(q, ^key)) > ^value))

      {:max, {:gt, value}} ->
        dynamic([q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:gte, value}}} ->
        dynamic([q], not (max(field(q, ^key)) >= ^value))

      {:max, {:gte, value}} ->
        dynamic([q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:lt, value}}} ->
        dynamic([q], not (max(field(q, ^key)) < ^value))

      {:max, {:lt, value}} ->
        dynamic([q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:lte, value}}} ->
        dynamic([q], not (max(field(q, ^key)) <= ^value))

      {:max, {:lte, value}} ->
        dynamic([q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:==, nil}}} ->
        dynamic([q], max(field(q, ^key)) != ^nil)

      {:max, {:==, nil}} ->
        dynamic([q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:==, value}}} ->
        dynamic([q], max(field(q, ^key)) != ^value)

      {:max, {:==, value}} ->
        dynamic([q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:eq, nil}}} ->
        dynamic([q], max(field(q, ^key)) != ^nil)

      {:max, {:eq, nil}} ->
        dynamic([q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:eq, value}}} ->
        dynamic([q], max(field(q, ^key)) != ^value)

      {:max, {:eq, value}} ->
        dynamic([q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:!=, nil}}} ->
        dynamic([q], max(field(q, ^key)) == ^nil)

      {:max, {:!=, nil}} ->
        dynamic([q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:!=, value}}} ->
        dynamic([q], max(field(q, ^key)) == ^value)

      {:max, {:!=, value}} ->
        dynamic([q], max(field(q, ^key)) != ^value)

      {:not, {:max, {:ne, nil}}} ->
        dynamic([q], max(field(q, ^key)) == ^nil)

      {:max, {:ne, nil}} ->
        dynamic([q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:ne, value}}} ->
        dynamic([q], max(field(q, ^key)) == ^value)

      {:max, {:ne, value}} ->
        dynamic([q], max(field(q, ^key)) != ^value)

      {:not, {:min, {:>, value}}} ->
        dynamic([q], not (min(field(q, ^key)) > ^value))

      {:min, {:>, value}} ->
        dynamic([q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:>=, value}}} ->
        dynamic([q], not (min(field(q, ^key)) >= ^value))

      {:min, {:>=, value}} ->
        dynamic([q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:<, value}}} ->
        dynamic([q], not (min(field(q, ^key)) < ^value))

      {:min, {:<, value}} ->
        dynamic([q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:<=, value}}} ->
        dynamic([q], not (min(field(q, ^key)) <= ^value))

      {:min, {:<=, value}} ->
        dynamic([q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:gt, value}}} ->
        dynamic([q], not (min(field(q, ^key)) > ^value))

      {:min, {:gt, value}} ->
        dynamic([q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:gte, value}}} ->
        dynamic([q], not (min(field(q, ^key)) >= ^value))

      {:min, {:gte, value}} ->
        dynamic([q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:lt, value}}} ->
        dynamic([q], not (min(field(q, ^key)) < ^value))

      {:min, {:lt, value}} ->
        dynamic([q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:lte, value}}} ->
        dynamic([q], not (min(field(q, ^key)) <= ^value))

      {:min, {:lte, value}} ->
        dynamic([q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:==, nil}}} ->
        dynamic([q], min(field(q, ^key)) != ^nil)

      {:min, {:==, nil}} ->
        dynamic([q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:==, value}}} ->
        dynamic([q], min(field(q, ^key)) != ^value)

      {:min, {:==, value}} ->
        dynamic([q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:eq, nil}}} ->
        dynamic([q], min(field(q, ^key)) != ^nil)

      {:min, {:eq, nil}} ->
        dynamic([q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:eq, value}}} ->
        dynamic([q], min(field(q, ^key)) != ^value)

      {:min, {:eq, value}} ->
        dynamic([q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:!=, nil}}} ->
        dynamic([q], min(field(q, ^key)) == ^nil)

      {:min, {:!=, nil}} ->
        dynamic([q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:!=, value}}} ->
        dynamic([q], min(field(q, ^key)) == ^value)

      {:min, {:!=, value}} ->
        dynamic([q], min(field(q, ^key)) != ^value)

      {:not, {:min, {:ne, nil}}} ->
        dynamic([q], min(field(q, ^key)) == ^nil)

      {:min, {:ne, nil}} ->
        dynamic([q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:ne, value}}} ->
        dynamic([q], min(field(q, ^key)) == ^value)

      {:min, {:ne, value}} ->
        dynamic([q], min(field(q, ^key)) != ^value)

      {:not, {:sum, {:>, value}}} ->
        dynamic([q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:>, value}} ->
        dynamic([q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:>=, value}}} ->
        dynamic([q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:>=, value}} ->
        dynamic([q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:<, value}}} ->
        dynamic([q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:<, value}} ->
        dynamic([q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:<=, value}}} ->
        dynamic([q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:<=, value}} ->
        dynamic([q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:gt, value}}} ->
        dynamic([q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:gt, value}} ->
        dynamic([q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:gte, value}}} ->
        dynamic([q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:gte, value}} ->
        dynamic([q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:lt, value}}} ->
        dynamic([q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:lt, value}} ->
        dynamic([q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:lte, value}}} ->
        dynamic([q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:lte, value}} ->
        dynamic([q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:==, nil}}} ->
        dynamic([q], sum(field(q, ^key)) != ^nil)

      {:sum, {:==, nil}} ->
        dynamic([q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:==, value}}} ->
        dynamic([q], sum(field(q, ^key)) != ^value)

      {:sum, {:==, value}} ->
        dynamic([q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:eq, nil}}} ->
        dynamic([q], sum(field(q, ^key)) != ^nil)

      {:sum, {:eq, nil}} ->
        dynamic([q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:eq, value}}} ->
        dynamic([q], sum(field(q, ^key)) != ^value)

      {:sum, {:eq, value}} ->
        dynamic([q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:!=, nil}}} ->
        dynamic([q], sum(field(q, ^key)) == ^nil)

      {:sum, {:!=, nil}} ->
        dynamic([q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:!=, value}}} ->
        dynamic([q], sum(field(q, ^key)) == ^value)

      {:sum, {:!=, value}} ->
        dynamic([q], sum(field(q, ^key)) != ^value)

      {:not, {:sum, {:ne, nil}}} ->
        dynamic([q], sum(field(q, ^key)) == ^nil)

      {:sum, {:ne, nil}} ->
        dynamic([q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:ne, value}}} ->
        dynamic([q], sum(field(q, ^key)) == ^value)

      {:sum, {:ne, value}} ->
        dynamic([q], sum(field(q, ^key)) != ^value)
    end
  end

  def dynamic_expr({:at, 2}, key, negated, value) do
    term =
      case negated do
        :not -> {:not, value}
        _ -> value
      end

    case term do
      {:not, {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:>, {:value, wrapped_value}}} ->
        dynamic([_, q], not (field(q, ^key) > ^wrapped_value))

      {:>, {:value, wrapped_value}} ->
        dynamic([_, q], field(q, ^key) > ^wrapped_value)

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")))

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")))

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")))

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")))

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")))

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")))

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>, value}} ->
        dynamic([_, q], not (field(q, ^key) > ^value))

      {:>, value} ->
        dynamic([_, q], field(q, ^key) > ^value)

      {:not, {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:>=, {:value, wrapped_value}}} ->
        dynamic([_, q], not (field(q, ^key) >= ^wrapped_value))

      {:>=, {:value, wrapped_value}} ->
        dynamic([_, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")))

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")))

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")))

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")))

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>=, value}} ->
        dynamic([_, q], not (field(q, ^key) >= ^value))

      {:>=, value} ->
        dynamic([_, q], field(q, ^key) >= ^value)

      {:not, {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:<, {:value, wrapped_value}}} ->
        dynamic([_, q], not (field(q, ^key) < ^wrapped_value))

      {:<, {:value, wrapped_value}} ->
        dynamic([_, q], field(q, ^key) < ^wrapped_value)

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")))

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")))

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")))

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")))

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")))

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")))

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<, value}} ->
        dynamic([_, q], not (field(q, ^key) < ^value))

      {:<, value} ->
        dynamic([_, q], field(q, ^key) < ^value)

      {:not, {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:<=, {:value, wrapped_value}}} ->
        dynamic([_, q], not (field(q, ^key) <= ^wrapped_value))

      {:<=, {:value, wrapped_value}} ->
        dynamic([_, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")))

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")))

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")))

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")))

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<=, value}} ->
        dynamic([_, q], not (field(q, ^key) <= ^value))

      {:<=, value} ->
        dynamic([_, q], field(q, ^key) <= ^value)

      {:not, {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:gt, {:value, wrapped_value}}} ->
        dynamic([_, q], not (field(q, ^key) > ^wrapped_value))

      {:gt, {:value, wrapped_value}} ->
        dynamic([_, q], field(q, ^key) > ^wrapped_value)

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")))

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")))

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")))

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")))

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")))

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")))

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gt, value}} ->
        dynamic([_, q], not (field(q, ^key) > ^value))

      {:gt, value} ->
        dynamic([_, q], field(q, ^key) > ^value)

      {:not, {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:gte, {:value, wrapped_value}}} ->
        dynamic([_, q], not (field(q, ^key) >= ^wrapped_value))

      {:gte, {:value, wrapped_value}} ->
        dynamic([_, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")))

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")))

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")))

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")))

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gte, value}} ->
        dynamic([_, q], not (field(q, ^key) >= ^value))

      {:gte, value} ->
        dynamic([_, q], field(q, ^key) >= ^value)

      {:not, {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:lt, {:value, wrapped_value}}} ->
        dynamic([_, q], not (field(q, ^key) < ^wrapped_value))

      {:lt, {:value, wrapped_value}} ->
        dynamic([_, q], field(q, ^key) < ^wrapped_value)

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")))

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")))

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")))

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")))

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")))

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")))

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lt, value}} ->
        dynamic([_, q], not (field(q, ^key) < ^value))

      {:lt, value} ->
        dynamic([_, q], field(q, ^key) < ^value)

      {:not, {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:lte, {:value, wrapped_value}}} ->
        dynamic([_, q], not (field(q, ^key) <= ^wrapped_value))

      {:lte, {:value, wrapped_value}} ->
        dynamic([_, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")))

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")))

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")))

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")))

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lte, value}} ->
        dynamic([_, q], not (field(q, ^key) <= ^value))

      {:lte, value} ->
        dynamic([_, q], field(q, ^key) <= ^value)

      {:not, {:==, {:all, quantified_value}}} ->
        dynamic([_, q], not (field(q, ^key) == all(quantified_value)))

      {:==, {:all, quantified_value}} ->
        dynamic([_, q], field(q, ^key) == all(quantified_value))

      {:not, {:==, {:any, quantified_value}}} ->
        dynamic([_, q], not (field(q, ^key) == any(quantified_value)))

      {:==, {:any, quantified_value}} ->
        dynamic([_, q], field(q, ^key) == any(quantified_value))

      {:not, {:==, nil}} ->
        dynamic([_, q], not is_nil(field(q, ^key)))

      {:==, nil} ->
        dynamic([_, q], is_nil(field(q, ^key)))

      {:not, {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([_, q], field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value})

      {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([_, q], field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value})

      {:not, {:==, {:value, wrapped_value}}} ->
        dynamic([_, q], field(q, ^key) != ^wrapped_value)

      {:==, {:value, wrapped_value}} ->
        dynamic([_, q], field(q, ^key) == ^wrapped_value)

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:==, value}} ->
        dynamic([_, q], field(q, ^key) != ^value)

      {:==, value} ->
        dynamic([_, q], field(q, ^key) == ^value)

      {:not, {:eq, {:all, quantified_value}}} ->
        dynamic([_, q], not (field(q, ^key) == all(quantified_value)))

      {:eq, {:all, quantified_value}} ->
        dynamic([_, q], field(q, ^key) == all(quantified_value))

      {:not, {:eq, {:any, quantified_value}}} ->
        dynamic([_, q], not (field(q, ^key) == any(quantified_value)))

      {:eq, {:any, quantified_value}} ->
        dynamic([_, q], field(q, ^key) == any(quantified_value))

      {:not, {:eq, nil}} ->
        dynamic([_, q], not is_nil(field(q, ^key)))

      {:eq, nil} ->
        dynamic([_, q], is_nil(field(q, ^key)))

      {:not, {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([_, q], field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value})

      {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([_, q], field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value})

      {:not, {:eq, {:value, wrapped_value}}} ->
        dynamic([_, q], field(q, ^key) != ^wrapped_value)

      {:eq, {:value, wrapped_value}} ->
        dynamic([_, q], field(q, ^key) == ^wrapped_value)

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:eq, value}} ->
        dynamic([_, q], field(q, ^key) != ^value)

      {:eq, value} ->
        dynamic([_, q], field(q, ^key) == ^value)

      {:not, {:!=, {:all, quantified_value}}} ->
        dynamic([_, q], not (field(q, ^key) != all(quantified_value)))

      {:!=, {:all, quantified_value}} ->
        dynamic([_, q], field(q, ^key) != all(quantified_value))

      {:not, {:!=, {:any, quantified_value}}} ->
        dynamic([_, q], not (field(q, ^key) != any(quantified_value)))

      {:!=, {:any, quantified_value}} ->
        dynamic([_, q], field(q, ^key) != any(quantified_value))

      {:not, {:!=, nil}} ->
        dynamic([_, q], is_nil(field(q, ^key)))

      {:!=, nil} ->
        dynamic([_, q], not is_nil(field(q, ^key)))

      {:not, {:!=, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([_, q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:!=, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([_, q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([_, q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:!=, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([_, q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([_, q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:!=, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([_, q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([_, q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:!=, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([_, q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, wrapped_value}}} ->
        dynamic([_, q], field(q, ^key) == ^wrapped_value)

      {:!=, {:value, wrapped_value}} ->
        dynamic([_, q], field(q, ^key) != ^wrapped_value)

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:!=, value}} ->
        dynamic([_, q], field(q, ^key) == ^value)

      {:!=, value} ->
        dynamic([_, q], field(q, ^key) != ^value)

      {:not, {:ne, {:all, quantified_value}}} ->
        dynamic([_, q], not (field(q, ^key) != all(quantified_value)))

      {:ne, {:all, quantified_value}} ->
        dynamic([_, q], field(q, ^key) != all(quantified_value))

      {:not, {:ne, {:any, quantified_value}}} ->
        dynamic([_, q], not (field(q, ^key) != any(quantified_value)))

      {:ne, {:any, quantified_value}} ->
        dynamic([_, q], field(q, ^key) != any(quantified_value))

      {:not, {:ne, nil}} ->
        dynamic([_, q], is_nil(field(q, ^key)))

      {:ne, nil} ->
        dynamic([_, q], not is_nil(field(q, ^key)))

      {:not, {:ne, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([_, q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:ne, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([_, q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([_, q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:ne, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([_, q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([_, q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:ne, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([_, q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([_, q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:ne, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([_, q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, wrapped_value}}} ->
        dynamic([_, q], field(q, ^key) == ^wrapped_value)

      {:ne, {:value, wrapped_value}} ->
        dynamic([_, q], field(q, ^key) != ^wrapped_value)

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:ne, value}} ->
        dynamic([_, q], field(q, ^key) == ^value)

      {:ne, value} ->
        dynamic([_, q], field(q, ^key) != ^value)

      {:not, {:avg, {:>, value}}} ->
        dynamic([_, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:>, value}} ->
        dynamic([_, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:>=, value}}} ->
        dynamic([_, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:>=, value}} ->
        dynamic([_, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:<, value}}} ->
        dynamic([_, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:<, value}} ->
        dynamic([_, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:<=, value}}} ->
        dynamic([_, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:<=, value}} ->
        dynamic([_, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:gt, value}}} ->
        dynamic([_, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:gt, value}} ->
        dynamic([_, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:gte, value}}} ->
        dynamic([_, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:gte, value}} ->
        dynamic([_, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:lt, value}}} ->
        dynamic([_, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:lt, value}} ->
        dynamic([_, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:lte, value}}} ->
        dynamic([_, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:lte, value}} ->
        dynamic([_, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:==, nil}}} ->
        dynamic([_, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:==, nil}} ->
        dynamic([_, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:==, value}}} ->
        dynamic([_, q], avg(field(q, ^key)) != ^value)

      {:avg, {:==, value}} ->
        dynamic([_, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:eq, nil}}} ->
        dynamic([_, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:eq, nil}} ->
        dynamic([_, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:eq, value}}} ->
        dynamic([_, q], avg(field(q, ^key)) != ^value)

      {:avg, {:eq, value}} ->
        dynamic([_, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:!=, nil}}} ->
        dynamic([_, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:!=, nil}} ->
        dynamic([_, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:!=, value}}} ->
        dynamic([_, q], avg(field(q, ^key)) == ^value)

      {:avg, {:!=, value}} ->
        dynamic([_, q], avg(field(q, ^key)) != ^value)

      {:not, {:avg, {:ne, nil}}} ->
        dynamic([_, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:ne, nil}} ->
        dynamic([_, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:ne, value}}} ->
        dynamic([_, q], avg(field(q, ^key)) == ^value)

      {:avg, {:ne, value}} ->
        dynamic([_, q], avg(field(q, ^key)) != ^value)

      {:not, {:count, {:>, value}}} ->
        dynamic([_, q], not (count(field(q, ^key)) > ^value))

      {:count, {:>, value}} ->
        dynamic([_, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:>=, value}}} ->
        dynamic([_, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:>=, value}} ->
        dynamic([_, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:<, value}}} ->
        dynamic([_, q], not (count(field(q, ^key)) < ^value))

      {:count, {:<, value}} ->
        dynamic([_, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:<=, value}}} ->
        dynamic([_, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:<=, value}} ->
        dynamic([_, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:gt, value}}} ->
        dynamic([_, q], not (count(field(q, ^key)) > ^value))

      {:count, {:gt, value}} ->
        dynamic([_, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:gte, value}}} ->
        dynamic([_, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:gte, value}} ->
        dynamic([_, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:lt, value}}} ->
        dynamic([_, q], not (count(field(q, ^key)) < ^value))

      {:count, {:lt, value}} ->
        dynamic([_, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:lte, value}}} ->
        dynamic([_, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:lte, value}} ->
        dynamic([_, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:==, nil}}} ->
        dynamic([_, q], count(field(q, ^key)) != ^nil)

      {:count, {:==, nil}} ->
        dynamic([_, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:==, value}}} ->
        dynamic([_, q], count(field(q, ^key)) != ^value)

      {:count, {:==, value}} ->
        dynamic([_, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:eq, nil}}} ->
        dynamic([_, q], count(field(q, ^key)) != ^nil)

      {:count, {:eq, nil}} ->
        dynamic([_, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:eq, value}}} ->
        dynamic([_, q], count(field(q, ^key)) != ^value)

      {:count, {:eq, value}} ->
        dynamic([_, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:!=, nil}}} ->
        dynamic([_, q], count(field(q, ^key)) == ^nil)

      {:count, {:!=, nil}} ->
        dynamic([_, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:!=, value}}} ->
        dynamic([_, q], count(field(q, ^key)) == ^value)

      {:count, {:!=, value}} ->
        dynamic([_, q], count(field(q, ^key)) != ^value)

      {:not, {:count, {:ne, nil}}} ->
        dynamic([_, q], count(field(q, ^key)) == ^nil)

      {:count, {:ne, nil}} ->
        dynamic([_, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:ne, value}}} ->
        dynamic([_, q], count(field(q, ^key)) == ^value)

      {:count, {:ne, value}} ->
        dynamic([_, q], count(field(q, ^key)) != ^value)

      {:not, {:max, {:>, value}}} ->
        dynamic([_, q], not (max(field(q, ^key)) > ^value))

      {:max, {:>, value}} ->
        dynamic([_, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:>=, value}}} ->
        dynamic([_, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:>=, value}} ->
        dynamic([_, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:<, value}}} ->
        dynamic([_, q], not (max(field(q, ^key)) < ^value))

      {:max, {:<, value}} ->
        dynamic([_, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:<=, value}}} ->
        dynamic([_, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:<=, value}} ->
        dynamic([_, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:gt, value}}} ->
        dynamic([_, q], not (max(field(q, ^key)) > ^value))

      {:max, {:gt, value}} ->
        dynamic([_, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:gte, value}}} ->
        dynamic([_, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:gte, value}} ->
        dynamic([_, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:lt, value}}} ->
        dynamic([_, q], not (max(field(q, ^key)) < ^value))

      {:max, {:lt, value}} ->
        dynamic([_, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:lte, value}}} ->
        dynamic([_, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:lte, value}} ->
        dynamic([_, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:==, nil}}} ->
        dynamic([_, q], max(field(q, ^key)) != ^nil)

      {:max, {:==, nil}} ->
        dynamic([_, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:==, value}}} ->
        dynamic([_, q], max(field(q, ^key)) != ^value)

      {:max, {:==, value}} ->
        dynamic([_, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:eq, nil}}} ->
        dynamic([_, q], max(field(q, ^key)) != ^nil)

      {:max, {:eq, nil}} ->
        dynamic([_, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:eq, value}}} ->
        dynamic([_, q], max(field(q, ^key)) != ^value)

      {:max, {:eq, value}} ->
        dynamic([_, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:!=, nil}}} ->
        dynamic([_, q], max(field(q, ^key)) == ^nil)

      {:max, {:!=, nil}} ->
        dynamic([_, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:!=, value}}} ->
        dynamic([_, q], max(field(q, ^key)) == ^value)

      {:max, {:!=, value}} ->
        dynamic([_, q], max(field(q, ^key)) != ^value)

      {:not, {:max, {:ne, nil}}} ->
        dynamic([_, q], max(field(q, ^key)) == ^nil)

      {:max, {:ne, nil}} ->
        dynamic([_, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:ne, value}}} ->
        dynamic([_, q], max(field(q, ^key)) == ^value)

      {:max, {:ne, value}} ->
        dynamic([_, q], max(field(q, ^key)) != ^value)

      {:not, {:min, {:>, value}}} ->
        dynamic([_, q], not (min(field(q, ^key)) > ^value))

      {:min, {:>, value}} ->
        dynamic([_, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:>=, value}}} ->
        dynamic([_, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:>=, value}} ->
        dynamic([_, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:<, value}}} ->
        dynamic([_, q], not (min(field(q, ^key)) < ^value))

      {:min, {:<, value}} ->
        dynamic([_, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:<=, value}}} ->
        dynamic([_, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:<=, value}} ->
        dynamic([_, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:gt, value}}} ->
        dynamic([_, q], not (min(field(q, ^key)) > ^value))

      {:min, {:gt, value}} ->
        dynamic([_, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:gte, value}}} ->
        dynamic([_, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:gte, value}} ->
        dynamic([_, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:lt, value}}} ->
        dynamic([_, q], not (min(field(q, ^key)) < ^value))

      {:min, {:lt, value}} ->
        dynamic([_, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:lte, value}}} ->
        dynamic([_, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:lte, value}} ->
        dynamic([_, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:==, nil}}} ->
        dynamic([_, q], min(field(q, ^key)) != ^nil)

      {:min, {:==, nil}} ->
        dynamic([_, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:==, value}}} ->
        dynamic([_, q], min(field(q, ^key)) != ^value)

      {:min, {:==, value}} ->
        dynamic([_, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:eq, nil}}} ->
        dynamic([_, q], min(field(q, ^key)) != ^nil)

      {:min, {:eq, nil}} ->
        dynamic([_, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:eq, value}}} ->
        dynamic([_, q], min(field(q, ^key)) != ^value)

      {:min, {:eq, value}} ->
        dynamic([_, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:!=, nil}}} ->
        dynamic([_, q], min(field(q, ^key)) == ^nil)

      {:min, {:!=, nil}} ->
        dynamic([_, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:!=, value}}} ->
        dynamic([_, q], min(field(q, ^key)) == ^value)

      {:min, {:!=, value}} ->
        dynamic([_, q], min(field(q, ^key)) != ^value)

      {:not, {:min, {:ne, nil}}} ->
        dynamic([_, q], min(field(q, ^key)) == ^nil)

      {:min, {:ne, nil}} ->
        dynamic([_, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:ne, value}}} ->
        dynamic([_, q], min(field(q, ^key)) == ^value)

      {:min, {:ne, value}} ->
        dynamic([_, q], min(field(q, ^key)) != ^value)

      {:not, {:sum, {:>, value}}} ->
        dynamic([_, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:>, value}} ->
        dynamic([_, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:>=, value}}} ->
        dynamic([_, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:>=, value}} ->
        dynamic([_, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:<, value}}} ->
        dynamic([_, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:<, value}} ->
        dynamic([_, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:<=, value}}} ->
        dynamic([_, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:<=, value}} ->
        dynamic([_, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:gt, value}}} ->
        dynamic([_, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:gt, value}} ->
        dynamic([_, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:gte, value}}} ->
        dynamic([_, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:gte, value}} ->
        dynamic([_, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:lt, value}}} ->
        dynamic([_, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:lt, value}} ->
        dynamic([_, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:lte, value}}} ->
        dynamic([_, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:lte, value}} ->
        dynamic([_, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:==, nil}}} ->
        dynamic([_, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:==, nil}} ->
        dynamic([_, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:==, value}}} ->
        dynamic([_, q], sum(field(q, ^key)) != ^value)

      {:sum, {:==, value}} ->
        dynamic([_, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:eq, nil}}} ->
        dynamic([_, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:eq, nil}} ->
        dynamic([_, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:eq, value}}} ->
        dynamic([_, q], sum(field(q, ^key)) != ^value)

      {:sum, {:eq, value}} ->
        dynamic([_, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:!=, nil}}} ->
        dynamic([_, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:!=, nil}} ->
        dynamic([_, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:!=, value}}} ->
        dynamic([_, q], sum(field(q, ^key)) == ^value)

      {:sum, {:!=, value}} ->
        dynamic([_, q], sum(field(q, ^key)) != ^value)

      {:not, {:sum, {:ne, nil}}} ->
        dynamic([_, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:ne, nil}} ->
        dynamic([_, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:ne, value}}} ->
        dynamic([_, q], sum(field(q, ^key)) == ^value)

      {:sum, {:ne, value}} ->
        dynamic([_, q], sum(field(q, ^key)) != ^value)
    end
  end

  def dynamic_expr({:at, 3}, key, negated, value) do
    term =
      case negated do
        :not -> {:not, value}
        _ -> value
      end

    case term do
      {:not, {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:>, {:value, wrapped_value}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ^wrapped_value))

      {:>, {:value, wrapped_value}} ->
        dynamic([_, _, q], field(q, ^key) > ^wrapped_value)

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>, value}} ->
        dynamic([_, _, q], not (field(q, ^key) > ^value))

      {:>, value} ->
        dynamic([_, _, q], field(q, ^key) > ^value)

      {:not, {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:>=, {:value, wrapped_value}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ^wrapped_value))

      {:>=, {:value, wrapped_value}} ->
        dynamic([_, _, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>=, value}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ^value))

      {:>=, value} ->
        dynamic([_, _, q], field(q, ^key) >= ^value)

      {:not, {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:<, {:value, wrapped_value}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ^wrapped_value))

      {:<, {:value, wrapped_value}} ->
        dynamic([_, _, q], field(q, ^key) < ^wrapped_value)

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<, value}} ->
        dynamic([_, _, q], not (field(q, ^key) < ^value))

      {:<, value} ->
        dynamic([_, _, q], field(q, ^key) < ^value)

      {:not, {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:<=, {:value, wrapped_value}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ^wrapped_value))

      {:<=, {:value, wrapped_value}} ->
        dynamic([_, _, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<=, value}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ^value))

      {:<=, value} ->
        dynamic([_, _, q], field(q, ^key) <= ^value)

      {:not, {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:gt, {:value, wrapped_value}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ^wrapped_value))

      {:gt, {:value, wrapped_value}} ->
        dynamic([_, _, q], field(q, ^key) > ^wrapped_value)

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gt, value}} ->
        dynamic([_, _, q], not (field(q, ^key) > ^value))

      {:gt, value} ->
        dynamic([_, _, q], field(q, ^key) > ^value)

      {:not, {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:gte, {:value, wrapped_value}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ^wrapped_value))

      {:gte, {:value, wrapped_value}} ->
        dynamic([_, _, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gte, value}} ->
        dynamic([_, _, q], not (field(q, ^key) >= ^value))

      {:gte, value} ->
        dynamic([_, _, q], field(q, ^key) >= ^value)

      {:not, {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:lt, {:value, wrapped_value}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ^wrapped_value))

      {:lt, {:value, wrapped_value}} ->
        dynamic([_, _, q], field(q, ^key) < ^wrapped_value)

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")))

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lt, value}} ->
        dynamic([_, _, q], not (field(q, ^key) < ^value))

      {:lt, value} ->
        dynamic([_, _, q], field(q, ^key) < ^value)

      {:not, {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:lte, {:value, wrapped_value}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ^wrapped_value))

      {:lte, {:value, wrapped_value}} ->
        dynamic([_, _, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lte, value}} ->
        dynamic([_, _, q], not (field(q, ^key) <= ^value))

      {:lte, value} ->
        dynamic([_, _, q], field(q, ^key) <= ^value)

      {:not, {:==, {:all, quantified_value}}} ->
        dynamic([_, _, q], not (field(q, ^key) == all(quantified_value)))

      {:==, {:all, quantified_value}} ->
        dynamic([_, _, q], field(q, ^key) == all(quantified_value))

      {:not, {:==, {:any, quantified_value}}} ->
        dynamic([_, _, q], not (field(q, ^key) == any(quantified_value)))

      {:==, {:any, quantified_value}} ->
        dynamic([_, _, q], field(q, ^key) == any(quantified_value))

      {:not, {:==, nil}} ->
        dynamic([_, _, q], not is_nil(field(q, ^key)))

      {:==, nil} ->
        dynamic([_, _, q], is_nil(field(q, ^key)))

      {:not, {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([_, _, q], field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value})

      {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([_, _, q], field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value})

      {:not, {:==, {:value, wrapped_value}}} ->
        dynamic([_, _, q], field(q, ^key) != ^wrapped_value)

      {:==, {:value, wrapped_value}} ->
        dynamic([_, _, q], field(q, ^key) == ^wrapped_value)

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:==, value}} ->
        dynamic([_, _, q], field(q, ^key) != ^value)

      {:==, value} ->
        dynamic([_, _, q], field(q, ^key) == ^value)

      {:not, {:eq, {:all, quantified_value}}} ->
        dynamic([_, _, q], not (field(q, ^key) == all(quantified_value)))

      {:eq, {:all, quantified_value}} ->
        dynamic([_, _, q], field(q, ^key) == all(quantified_value))

      {:not, {:eq, {:any, quantified_value}}} ->
        dynamic([_, _, q], not (field(q, ^key) == any(quantified_value)))

      {:eq, {:any, quantified_value}} ->
        dynamic([_, _, q], field(q, ^key) == any(quantified_value))

      {:not, {:eq, nil}} ->
        dynamic([_, _, q], not is_nil(field(q, ^key)))

      {:eq, nil} ->
        dynamic([_, _, q], is_nil(field(q, ^key)))

      {:not, {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([_, _, q], field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value})

      {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([_, _, q], field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value})

      {:not, {:eq, {:value, wrapped_value}}} ->
        dynamic([_, _, q], field(q, ^key) != ^wrapped_value)

      {:eq, {:value, wrapped_value}} ->
        dynamic([_, _, q], field(q, ^key) == ^wrapped_value)

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:eq, value}} ->
        dynamic([_, _, q], field(q, ^key) != ^value)

      {:eq, value} ->
        dynamic([_, _, q], field(q, ^key) == ^value)

      {:not, {:!=, {:all, quantified_value}}} ->
        dynamic([_, _, q], not (field(q, ^key) != all(quantified_value)))

      {:!=, {:all, quantified_value}} ->
        dynamic([_, _, q], field(q, ^key) != all(quantified_value))

      {:not, {:!=, {:any, quantified_value}}} ->
        dynamic([_, _, q], not (field(q, ^key) != any(quantified_value)))

      {:!=, {:any, quantified_value}} ->
        dynamic([_, _, q], field(q, ^key) != any(quantified_value))

      {:not, {:!=, nil}} ->
        dynamic([_, _, q], is_nil(field(q, ^key)))

      {:!=, nil} ->
        dynamic([_, _, q], not is_nil(field(q, ^key)))

      {:not, {:!=, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:!=, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:!=, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:!=, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:!=, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, wrapped_value}}} ->
        dynamic([_, _, q], field(q, ^key) == ^wrapped_value)

      {:!=, {:value, wrapped_value}} ->
        dynamic([_, _, q], field(q, ^key) != ^wrapped_value)

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:!=, value}} ->
        dynamic([_, _, q], field(q, ^key) == ^value)

      {:!=, value} ->
        dynamic([_, _, q], field(q, ^key) != ^value)

      {:not, {:ne, {:all, quantified_value}}} ->
        dynamic([_, _, q], not (field(q, ^key) != all(quantified_value)))

      {:ne, {:all, quantified_value}} ->
        dynamic([_, _, q], field(q, ^key) != all(quantified_value))

      {:not, {:ne, {:any, quantified_value}}} ->
        dynamic([_, _, q], not (field(q, ^key) != any(quantified_value)))

      {:ne, {:any, quantified_value}} ->
        dynamic([_, _, q], field(q, ^key) != any(quantified_value))

      {:not, {:ne, nil}} ->
        dynamic([_, _, q], is_nil(field(q, ^key)))

      {:ne, nil} ->
        dynamic([_, _, q], not is_nil(field(q, ^key)))

      {:not, {:ne, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:ne, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:ne, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:ne, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:ne, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, wrapped_value}}} ->
        dynamic([_, _, q], field(q, ^key) == ^wrapped_value)

      {:ne, {:value, wrapped_value}} ->
        dynamic([_, _, q], field(q, ^key) != ^wrapped_value)

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:ne, value}} ->
        dynamic([_, _, q], field(q, ^key) == ^value)

      {:ne, value} ->
        dynamic([_, _, q], field(q, ^key) != ^value)

      {:not, {:avg, {:>, value}}} ->
        dynamic([_, _, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:>, value}} ->
        dynamic([_, _, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:>=, value}}} ->
        dynamic([_, _, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:>=, value}} ->
        dynamic([_, _, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:<, value}}} ->
        dynamic([_, _, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:<, value}} ->
        dynamic([_, _, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:<=, value}}} ->
        dynamic([_, _, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:<=, value}} ->
        dynamic([_, _, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:gt, value}}} ->
        dynamic([_, _, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:gt, value}} ->
        dynamic([_, _, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:gte, value}}} ->
        dynamic([_, _, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:gte, value}} ->
        dynamic([_, _, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:lt, value}}} ->
        dynamic([_, _, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:lt, value}} ->
        dynamic([_, _, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:lte, value}}} ->
        dynamic([_, _, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:lte, value}} ->
        dynamic([_, _, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:==, nil}}} ->
        dynamic([_, _, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:==, nil}} ->
        dynamic([_, _, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:==, value}}} ->
        dynamic([_, _, q], avg(field(q, ^key)) != ^value)

      {:avg, {:==, value}} ->
        dynamic([_, _, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:eq, nil}}} ->
        dynamic([_, _, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:eq, nil}} ->
        dynamic([_, _, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:eq, value}}} ->
        dynamic([_, _, q], avg(field(q, ^key)) != ^value)

      {:avg, {:eq, value}} ->
        dynamic([_, _, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:!=, nil}}} ->
        dynamic([_, _, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:!=, nil}} ->
        dynamic([_, _, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:!=, value}}} ->
        dynamic([_, _, q], avg(field(q, ^key)) == ^value)

      {:avg, {:!=, value}} ->
        dynamic([_, _, q], avg(field(q, ^key)) != ^value)

      {:not, {:avg, {:ne, nil}}} ->
        dynamic([_, _, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:ne, nil}} ->
        dynamic([_, _, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:ne, value}}} ->
        dynamic([_, _, q], avg(field(q, ^key)) == ^value)

      {:avg, {:ne, value}} ->
        dynamic([_, _, q], avg(field(q, ^key)) != ^value)

      {:not, {:count, {:>, value}}} ->
        dynamic([_, _, q], not (count(field(q, ^key)) > ^value))

      {:count, {:>, value}} ->
        dynamic([_, _, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:>=, value}}} ->
        dynamic([_, _, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:>=, value}} ->
        dynamic([_, _, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:<, value}}} ->
        dynamic([_, _, q], not (count(field(q, ^key)) < ^value))

      {:count, {:<, value}} ->
        dynamic([_, _, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:<=, value}}} ->
        dynamic([_, _, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:<=, value}} ->
        dynamic([_, _, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:gt, value}}} ->
        dynamic([_, _, q], not (count(field(q, ^key)) > ^value))

      {:count, {:gt, value}} ->
        dynamic([_, _, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:gte, value}}} ->
        dynamic([_, _, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:gte, value}} ->
        dynamic([_, _, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:lt, value}}} ->
        dynamic([_, _, q], not (count(field(q, ^key)) < ^value))

      {:count, {:lt, value}} ->
        dynamic([_, _, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:lte, value}}} ->
        dynamic([_, _, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:lte, value}} ->
        dynamic([_, _, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:==, nil}}} ->
        dynamic([_, _, q], count(field(q, ^key)) != ^nil)

      {:count, {:==, nil}} ->
        dynamic([_, _, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:==, value}}} ->
        dynamic([_, _, q], count(field(q, ^key)) != ^value)

      {:count, {:==, value}} ->
        dynamic([_, _, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:eq, nil}}} ->
        dynamic([_, _, q], count(field(q, ^key)) != ^nil)

      {:count, {:eq, nil}} ->
        dynamic([_, _, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:eq, value}}} ->
        dynamic([_, _, q], count(field(q, ^key)) != ^value)

      {:count, {:eq, value}} ->
        dynamic([_, _, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:!=, nil}}} ->
        dynamic([_, _, q], count(field(q, ^key)) == ^nil)

      {:count, {:!=, nil}} ->
        dynamic([_, _, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:!=, value}}} ->
        dynamic([_, _, q], count(field(q, ^key)) == ^value)

      {:count, {:!=, value}} ->
        dynamic([_, _, q], count(field(q, ^key)) != ^value)

      {:not, {:count, {:ne, nil}}} ->
        dynamic([_, _, q], count(field(q, ^key)) == ^nil)

      {:count, {:ne, nil}} ->
        dynamic([_, _, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:ne, value}}} ->
        dynamic([_, _, q], count(field(q, ^key)) == ^value)

      {:count, {:ne, value}} ->
        dynamic([_, _, q], count(field(q, ^key)) != ^value)

      {:not, {:max, {:>, value}}} ->
        dynamic([_, _, q], not (max(field(q, ^key)) > ^value))

      {:max, {:>, value}} ->
        dynamic([_, _, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:>=, value}}} ->
        dynamic([_, _, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:>=, value}} ->
        dynamic([_, _, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:<, value}}} ->
        dynamic([_, _, q], not (max(field(q, ^key)) < ^value))

      {:max, {:<, value}} ->
        dynamic([_, _, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:<=, value}}} ->
        dynamic([_, _, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:<=, value}} ->
        dynamic([_, _, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:gt, value}}} ->
        dynamic([_, _, q], not (max(field(q, ^key)) > ^value))

      {:max, {:gt, value}} ->
        dynamic([_, _, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:gte, value}}} ->
        dynamic([_, _, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:gte, value}} ->
        dynamic([_, _, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:lt, value}}} ->
        dynamic([_, _, q], not (max(field(q, ^key)) < ^value))

      {:max, {:lt, value}} ->
        dynamic([_, _, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:lte, value}}} ->
        dynamic([_, _, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:lte, value}} ->
        dynamic([_, _, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:==, nil}}} ->
        dynamic([_, _, q], max(field(q, ^key)) != ^nil)

      {:max, {:==, nil}} ->
        dynamic([_, _, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:==, value}}} ->
        dynamic([_, _, q], max(field(q, ^key)) != ^value)

      {:max, {:==, value}} ->
        dynamic([_, _, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:eq, nil}}} ->
        dynamic([_, _, q], max(field(q, ^key)) != ^nil)

      {:max, {:eq, nil}} ->
        dynamic([_, _, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:eq, value}}} ->
        dynamic([_, _, q], max(field(q, ^key)) != ^value)

      {:max, {:eq, value}} ->
        dynamic([_, _, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:!=, nil}}} ->
        dynamic([_, _, q], max(field(q, ^key)) == ^nil)

      {:max, {:!=, nil}} ->
        dynamic([_, _, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:!=, value}}} ->
        dynamic([_, _, q], max(field(q, ^key)) == ^value)

      {:max, {:!=, value}} ->
        dynamic([_, _, q], max(field(q, ^key)) != ^value)

      {:not, {:max, {:ne, nil}}} ->
        dynamic([_, _, q], max(field(q, ^key)) == ^nil)

      {:max, {:ne, nil}} ->
        dynamic([_, _, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:ne, value}}} ->
        dynamic([_, _, q], max(field(q, ^key)) == ^value)

      {:max, {:ne, value}} ->
        dynamic([_, _, q], max(field(q, ^key)) != ^value)

      {:not, {:min, {:>, value}}} ->
        dynamic([_, _, q], not (min(field(q, ^key)) > ^value))

      {:min, {:>, value}} ->
        dynamic([_, _, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:>=, value}}} ->
        dynamic([_, _, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:>=, value}} ->
        dynamic([_, _, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:<, value}}} ->
        dynamic([_, _, q], not (min(field(q, ^key)) < ^value))

      {:min, {:<, value}} ->
        dynamic([_, _, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:<=, value}}} ->
        dynamic([_, _, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:<=, value}} ->
        dynamic([_, _, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:gt, value}}} ->
        dynamic([_, _, q], not (min(field(q, ^key)) > ^value))

      {:min, {:gt, value}} ->
        dynamic([_, _, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:gte, value}}} ->
        dynamic([_, _, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:gte, value}} ->
        dynamic([_, _, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:lt, value}}} ->
        dynamic([_, _, q], not (min(field(q, ^key)) < ^value))

      {:min, {:lt, value}} ->
        dynamic([_, _, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:lte, value}}} ->
        dynamic([_, _, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:lte, value}} ->
        dynamic([_, _, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:==, nil}}} ->
        dynamic([_, _, q], min(field(q, ^key)) != ^nil)

      {:min, {:==, nil}} ->
        dynamic([_, _, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:==, value}}} ->
        dynamic([_, _, q], min(field(q, ^key)) != ^value)

      {:min, {:==, value}} ->
        dynamic([_, _, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:eq, nil}}} ->
        dynamic([_, _, q], min(field(q, ^key)) != ^nil)

      {:min, {:eq, nil}} ->
        dynamic([_, _, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:eq, value}}} ->
        dynamic([_, _, q], min(field(q, ^key)) != ^value)

      {:min, {:eq, value}} ->
        dynamic([_, _, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:!=, nil}}} ->
        dynamic([_, _, q], min(field(q, ^key)) == ^nil)

      {:min, {:!=, nil}} ->
        dynamic([_, _, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:!=, value}}} ->
        dynamic([_, _, q], min(field(q, ^key)) == ^value)

      {:min, {:!=, value}} ->
        dynamic([_, _, q], min(field(q, ^key)) != ^value)

      {:not, {:min, {:ne, nil}}} ->
        dynamic([_, _, q], min(field(q, ^key)) == ^nil)

      {:min, {:ne, nil}} ->
        dynamic([_, _, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:ne, value}}} ->
        dynamic([_, _, q], min(field(q, ^key)) == ^value)

      {:min, {:ne, value}} ->
        dynamic([_, _, q], min(field(q, ^key)) != ^value)

      {:not, {:sum, {:>, value}}} ->
        dynamic([_, _, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:>, value}} ->
        dynamic([_, _, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:>=, value}}} ->
        dynamic([_, _, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:>=, value}} ->
        dynamic([_, _, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:<, value}}} ->
        dynamic([_, _, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:<, value}} ->
        dynamic([_, _, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:<=, value}}} ->
        dynamic([_, _, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:<=, value}} ->
        dynamic([_, _, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:gt, value}}} ->
        dynamic([_, _, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:gt, value}} ->
        dynamic([_, _, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:gte, value}}} ->
        dynamic([_, _, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:gte, value}} ->
        dynamic([_, _, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:lt, value}}} ->
        dynamic([_, _, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:lt, value}} ->
        dynamic([_, _, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:lte, value}}} ->
        dynamic([_, _, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:lte, value}} ->
        dynamic([_, _, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:==, nil}}} ->
        dynamic([_, _, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:==, nil}} ->
        dynamic([_, _, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:==, value}}} ->
        dynamic([_, _, q], sum(field(q, ^key)) != ^value)

      {:sum, {:==, value}} ->
        dynamic([_, _, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:eq, nil}}} ->
        dynamic([_, _, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:eq, nil}} ->
        dynamic([_, _, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:eq, value}}} ->
        dynamic([_, _, q], sum(field(q, ^key)) != ^value)

      {:sum, {:eq, value}} ->
        dynamic([_, _, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:!=, nil}}} ->
        dynamic([_, _, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:!=, nil}} ->
        dynamic([_, _, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:!=, value}}} ->
        dynamic([_, _, q], sum(field(q, ^key)) == ^value)

      {:sum, {:!=, value}} ->
        dynamic([_, _, q], sum(field(q, ^key)) != ^value)

      {:not, {:sum, {:ne, nil}}} ->
        dynamic([_, _, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:ne, nil}} ->
        dynamic([_, _, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:ne, value}}} ->
        dynamic([_, _, q], sum(field(q, ^key)) == ^value)

      {:sum, {:ne, value}} ->
        dynamic([_, _, q], sum(field(q, ^key)) != ^value)
    end
  end

  def dynamic_expr({:at, 4}, key, negated, value) do
    term =
      case negated do
        :not -> {:not, value}
        _ -> value
      end

    case term do
      {:not, {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:>, {:value, wrapped_value}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ^wrapped_value))

      {:>, {:value, wrapped_value}} ->
        dynamic([_, _, _, q], field(q, ^key) > ^wrapped_value)

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>, value}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ^value))

      {:>, value} ->
        dynamic([_, _, _, q], field(q, ^key) > ^value)

      {:not, {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:>=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ^wrapped_value))

      {:>=, {:value, wrapped_value}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>=, value}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ^value))

      {:>=, value} ->
        dynamic([_, _, _, q], field(q, ^key) >= ^value)

      {:not, {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:<, {:value, wrapped_value}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ^wrapped_value))

      {:<, {:value, wrapped_value}} ->
        dynamic([_, _, _, q], field(q, ^key) < ^wrapped_value)

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<, value}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ^value))

      {:<, value} ->
        dynamic([_, _, _, q], field(q, ^key) < ^value)

      {:not, {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:<=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ^wrapped_value))

      {:<=, {:value, wrapped_value}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<=, value}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ^value))

      {:<=, value} ->
        dynamic([_, _, _, q], field(q, ^key) <= ^value)

      {:not, {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:gt, {:value, wrapped_value}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ^wrapped_value))

      {:gt, {:value, wrapped_value}} ->
        dynamic([_, _, _, q], field(q, ^key) > ^wrapped_value)

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gt, value}} ->
        dynamic([_, _, _, q], not (field(q, ^key) > ^value))

      {:gt, value} ->
        dynamic([_, _, _, q], field(q, ^key) > ^value)

      {:not, {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:gte, {:value, wrapped_value}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ^wrapped_value))

      {:gte, {:value, wrapped_value}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gte, value}} ->
        dynamic([_, _, _, q], not (field(q, ^key) >= ^value))

      {:gte, value} ->
        dynamic([_, _, _, q], field(q, ^key) >= ^value)

      {:not, {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:lt, {:value, wrapped_value}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ^wrapped_value))

      {:lt, {:value, wrapped_value}} ->
        dynamic([_, _, _, q], field(q, ^key) < ^wrapped_value)

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lt, value}} ->
        dynamic([_, _, _, q], not (field(q, ^key) < ^value))

      {:lt, value} ->
        dynamic([_, _, _, q], field(q, ^key) < ^value)

      {:not, {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:lte, {:value, wrapped_value}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ^wrapped_value))

      {:lte, {:value, wrapped_value}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lte, value}} ->
        dynamic([_, _, _, q], not (field(q, ^key) <= ^value))

      {:lte, value} ->
        dynamic([_, _, _, q], field(q, ^key) <= ^value)

      {:not, {:==, {:all, quantified_value}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) == all(quantified_value)))

      {:==, {:all, quantified_value}} ->
        dynamic([_, _, _, q], field(q, ^key) == all(quantified_value))

      {:not, {:==, {:any, quantified_value}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) == any(quantified_value)))

      {:==, {:any, quantified_value}} ->
        dynamic([_, _, _, q], field(q, ^key) == any(quantified_value))

      {:not, {:==, nil}} ->
        dynamic([_, _, _, q], not is_nil(field(q, ^key)))

      {:==, nil} ->
        dynamic([_, _, _, q], is_nil(field(q, ^key)))

      {:not, {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([_, _, _, q], field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value})

      {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([_, _, _, q], field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value})

      {:not, {:==, {:value, wrapped_value}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ^wrapped_value)

      {:==, {:value, wrapped_value}} ->
        dynamic([_, _, _, q], field(q, ^key) == ^wrapped_value)

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:==, value}} ->
        dynamic([_, _, _, q], field(q, ^key) != ^value)

      {:==, value} ->
        dynamic([_, _, _, q], field(q, ^key) == ^value)

      {:not, {:eq, {:all, quantified_value}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) == all(quantified_value)))

      {:eq, {:all, quantified_value}} ->
        dynamic([_, _, _, q], field(q, ^key) == all(quantified_value))

      {:not, {:eq, {:any, quantified_value}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) == any(quantified_value)))

      {:eq, {:any, quantified_value}} ->
        dynamic([_, _, _, q], field(q, ^key) == any(quantified_value))

      {:not, {:eq, nil}} ->
        dynamic([_, _, _, q], not is_nil(field(q, ^key)))

      {:eq, nil} ->
        dynamic([_, _, _, q], is_nil(field(q, ^key)))

      {:not, {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([_, _, _, q], field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value})

      {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([_, _, _, q], field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value})

      {:not, {:eq, {:value, wrapped_value}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ^wrapped_value)

      {:eq, {:value, wrapped_value}} ->
        dynamic([_, _, _, q], field(q, ^key) == ^wrapped_value)

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:eq, value}} ->
        dynamic([_, _, _, q], field(q, ^key) != ^value)

      {:eq, value} ->
        dynamic([_, _, _, q], field(q, ^key) == ^value)

      {:not, {:!=, {:all, quantified_value}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) != all(quantified_value)))

      {:!=, {:all, quantified_value}} ->
        dynamic([_, _, _, q], field(q, ^key) != all(quantified_value))

      {:not, {:!=, {:any, quantified_value}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) != any(quantified_value)))

      {:!=, {:any, quantified_value}} ->
        dynamic([_, _, _, q], field(q, ^key) != any(quantified_value))

      {:not, {:!=, nil}} ->
        dynamic([_, _, _, q], is_nil(field(q, ^key)))

      {:!=, nil} ->
        dynamic([_, _, _, q], not is_nil(field(q, ^key)))

      {:not, {:!=, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:!=, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:!=, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:!=, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:!=, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ^wrapped_value)

      {:!=, {:value, wrapped_value}} ->
        dynamic([_, _, _, q], field(q, ^key) != ^wrapped_value)

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:!=, value}} ->
        dynamic([_, _, _, q], field(q, ^key) == ^value)

      {:!=, value} ->
        dynamic([_, _, _, q], field(q, ^key) != ^value)

      {:not, {:ne, {:all, quantified_value}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) != all(quantified_value)))

      {:ne, {:all, quantified_value}} ->
        dynamic([_, _, _, q], field(q, ^key) != all(quantified_value))

      {:not, {:ne, {:any, quantified_value}}} ->
        dynamic([_, _, _, q], not (field(q, ^key) != any(quantified_value)))

      {:ne, {:any, quantified_value}} ->
        dynamic([_, _, _, q], field(q, ^key) != any(quantified_value))

      {:not, {:ne, nil}} ->
        dynamic([_, _, _, q], is_nil(field(q, ^key)))

      {:ne, nil} ->
        dynamic([_, _, _, q], not is_nil(field(q, ^key)))

      {:not, {:ne, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:ne, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:ne, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:ne, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:ne, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, wrapped_value}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ^wrapped_value)

      {:ne, {:value, wrapped_value}} ->
        dynamic([_, _, _, q], field(q, ^key) != ^wrapped_value)

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour"))

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:ne, value}} ->
        dynamic([_, _, _, q], field(q, ^key) == ^value)

      {:ne, value} ->
        dynamic([_, _, _, q], field(q, ^key) != ^value)

      {:not, {:avg, {:>, value}}} ->
        dynamic([_, _, _, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:>, value}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:>=, value}}} ->
        dynamic([_, _, _, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:>=, value}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:<, value}}} ->
        dynamic([_, _, _, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:<, value}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:<=, value}}} ->
        dynamic([_, _, _, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:<=, value}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:gt, value}}} ->
        dynamic([_, _, _, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:gt, value}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:gte, value}}} ->
        dynamic([_, _, _, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:gte, value}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:lt, value}}} ->
        dynamic([_, _, _, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:lt, value}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:lte, value}}} ->
        dynamic([_, _, _, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:lte, value}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:==, nil}}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:==, nil}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:==, value}}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) != ^value)

      {:avg, {:==, value}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:eq, nil}}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:eq, nil}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:eq, value}}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) != ^value)

      {:avg, {:eq, value}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:!=, nil}}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:!=, nil}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:!=, value}}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) == ^value)

      {:avg, {:!=, value}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) != ^value)

      {:not, {:avg, {:ne, nil}}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:ne, nil}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:ne, value}}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) == ^value)

      {:avg, {:ne, value}} ->
        dynamic([_, _, _, q], avg(field(q, ^key)) != ^value)

      {:not, {:count, {:>, value}}} ->
        dynamic([_, _, _, q], not (count(field(q, ^key)) > ^value))

      {:count, {:>, value}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:>=, value}}} ->
        dynamic([_, _, _, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:>=, value}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:<, value}}} ->
        dynamic([_, _, _, q], not (count(field(q, ^key)) < ^value))

      {:count, {:<, value}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:<=, value}}} ->
        dynamic([_, _, _, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:<=, value}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:gt, value}}} ->
        dynamic([_, _, _, q], not (count(field(q, ^key)) > ^value))

      {:count, {:gt, value}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:gte, value}}} ->
        dynamic([_, _, _, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:gte, value}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:lt, value}}} ->
        dynamic([_, _, _, q], not (count(field(q, ^key)) < ^value))

      {:count, {:lt, value}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:lte, value}}} ->
        dynamic([_, _, _, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:lte, value}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:==, nil}}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) != ^nil)

      {:count, {:==, nil}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:==, value}}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) != ^value)

      {:count, {:==, value}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:eq, nil}}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) != ^nil)

      {:count, {:eq, nil}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:eq, value}}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) != ^value)

      {:count, {:eq, value}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:!=, nil}}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) == ^nil)

      {:count, {:!=, nil}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:!=, value}}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) == ^value)

      {:count, {:!=, value}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) != ^value)

      {:not, {:count, {:ne, nil}}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) == ^nil)

      {:count, {:ne, nil}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:ne, value}}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) == ^value)

      {:count, {:ne, value}} ->
        dynamic([_, _, _, q], count(field(q, ^key)) != ^value)

      {:not, {:max, {:>, value}}} ->
        dynamic([_, _, _, q], not (max(field(q, ^key)) > ^value))

      {:max, {:>, value}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:>=, value}}} ->
        dynamic([_, _, _, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:>=, value}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:<, value}}} ->
        dynamic([_, _, _, q], not (max(field(q, ^key)) < ^value))

      {:max, {:<, value}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:<=, value}}} ->
        dynamic([_, _, _, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:<=, value}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:gt, value}}} ->
        dynamic([_, _, _, q], not (max(field(q, ^key)) > ^value))

      {:max, {:gt, value}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:gte, value}}} ->
        dynamic([_, _, _, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:gte, value}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:lt, value}}} ->
        dynamic([_, _, _, q], not (max(field(q, ^key)) < ^value))

      {:max, {:lt, value}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:lte, value}}} ->
        dynamic([_, _, _, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:lte, value}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:==, nil}}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) != ^nil)

      {:max, {:==, nil}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:==, value}}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) != ^value)

      {:max, {:==, value}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:eq, nil}}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) != ^nil)

      {:max, {:eq, nil}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:eq, value}}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) != ^value)

      {:max, {:eq, value}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:!=, nil}}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) == ^nil)

      {:max, {:!=, nil}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:!=, value}}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) == ^value)

      {:max, {:!=, value}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) != ^value)

      {:not, {:max, {:ne, nil}}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) == ^nil)

      {:max, {:ne, nil}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:ne, value}}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) == ^value)

      {:max, {:ne, value}} ->
        dynamic([_, _, _, q], max(field(q, ^key)) != ^value)

      {:not, {:min, {:>, value}}} ->
        dynamic([_, _, _, q], not (min(field(q, ^key)) > ^value))

      {:min, {:>, value}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:>=, value}}} ->
        dynamic([_, _, _, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:>=, value}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:<, value}}} ->
        dynamic([_, _, _, q], not (min(field(q, ^key)) < ^value))

      {:min, {:<, value}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:<=, value}}} ->
        dynamic([_, _, _, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:<=, value}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:gt, value}}} ->
        dynamic([_, _, _, q], not (min(field(q, ^key)) > ^value))

      {:min, {:gt, value}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:gte, value}}} ->
        dynamic([_, _, _, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:gte, value}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:lt, value}}} ->
        dynamic([_, _, _, q], not (min(field(q, ^key)) < ^value))

      {:min, {:lt, value}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:lte, value}}} ->
        dynamic([_, _, _, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:lte, value}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:==, nil}}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) != ^nil)

      {:min, {:==, nil}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:==, value}}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) != ^value)

      {:min, {:==, value}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:eq, nil}}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) != ^nil)

      {:min, {:eq, nil}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:eq, value}}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) != ^value)

      {:min, {:eq, value}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:!=, nil}}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) == ^nil)

      {:min, {:!=, nil}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:!=, value}}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) == ^value)

      {:min, {:!=, value}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) != ^value)

      {:not, {:min, {:ne, nil}}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) == ^nil)

      {:min, {:ne, nil}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:ne, value}}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) == ^value)

      {:min, {:ne, value}} ->
        dynamic([_, _, _, q], min(field(q, ^key)) != ^value)

      {:not, {:sum, {:>, value}}} ->
        dynamic([_, _, _, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:>, value}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:>=, value}}} ->
        dynamic([_, _, _, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:>=, value}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:<, value}}} ->
        dynamic([_, _, _, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:<, value}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:<=, value}}} ->
        dynamic([_, _, _, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:<=, value}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:gt, value}}} ->
        dynamic([_, _, _, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:gt, value}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:gte, value}}} ->
        dynamic([_, _, _, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:gte, value}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:lt, value}}} ->
        dynamic([_, _, _, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:lt, value}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:lte, value}}} ->
        dynamic([_, _, _, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:lte, value}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:==, nil}}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:==, nil}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:==, value}}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) != ^value)

      {:sum, {:==, value}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:eq, nil}}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:eq, nil}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:eq, value}}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) != ^value)

      {:sum, {:eq, value}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:!=, nil}}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:!=, nil}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:!=, value}}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) == ^value)

      {:sum, {:!=, value}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) != ^value)

      {:not, {:sum, {:ne, nil}}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:ne, nil}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:ne, value}}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) == ^value)

      {:sum, {:ne, value}} ->
        dynamic([_, _, _, q], sum(field(q, ^key)) != ^value)
    end
  end

  def dynamic_expr({:at, 5}, key, negated, value) do
    term =
      case negated do
        :not -> {:not, value}
        _ -> value
      end

    case term do
      {:not, {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:>, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ^wrapped_value))

      {:>, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ^wrapped_value)

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>, value}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ^value))

      {:>, value} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ^value)

      {:not, {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:>=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ^wrapped_value))

      {:>=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>=, value}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ^value))

      {:>=, value} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ^value)

      {:not, {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:<, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ^wrapped_value))

      {:<, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ^wrapped_value)

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<, value}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ^value))

      {:<, value} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ^value)

      {:not, {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:<=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ^wrapped_value))

      {:<=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<=, value}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ^value))

      {:<=, value} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ^value)

      {:not, {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:gt, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ^wrapped_value))

      {:gt, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ^wrapped_value)

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gt, value}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) > ^value))

      {:gt, value} ->
        dynamic([_, _, _, _, q], field(q, ^key) > ^value)

      {:not, {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:gte, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ^wrapped_value))

      {:gte, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gte, value}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) >= ^value))

      {:gte, value} ->
        dynamic([_, _, _, _, q], field(q, ^key) >= ^value)

      {:not, {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:lt, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ^wrapped_value))

      {:lt, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ^wrapped_value)

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lt, value}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) < ^value))

      {:lt, value} ->
        dynamic([_, _, _, _, q], field(q, ^key) < ^value)

      {:not, {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:lte, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ^wrapped_value))

      {:lte, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lte, value}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) <= ^value))

      {:lte, value} ->
        dynamic([_, _, _, _, q], field(q, ^key) <= ^value)

      {:not, {:==, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) == all(quantified_value)))

      {:==, {:all, quantified_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == all(quantified_value))

      {:not, {:==, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) == any(quantified_value)))

      {:==, {:any, quantified_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == any(quantified_value))

      {:not, {:==, nil}} ->
        dynamic([_, _, _, _, q], not is_nil(field(q, ^key)))

      {:==, nil} ->
        dynamic([_, _, _, _, q], is_nil(field(q, ^key)))

      {:not, {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value})

      {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value})

      {:not, {:==, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:==, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:==, value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^value)

      {:==, value} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^value)

      {:not, {:eq, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) == all(quantified_value)))

      {:eq, {:all, quantified_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == all(quantified_value))

      {:not, {:eq, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) == any(quantified_value)))

      {:eq, {:any, quantified_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == any(quantified_value))

      {:not, {:eq, nil}} ->
        dynamic([_, _, _, _, q], not is_nil(field(q, ^key)))

      {:eq, nil} ->
        dynamic([_, _, _, _, q], is_nil(field(q, ^key)))

      {:not, {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value})

      {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value})

      {:not, {:eq, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:eq, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:eq, value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^value)

      {:eq, value} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^value)

      {:not, {:!=, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) != all(quantified_value)))

      {:!=, {:all, quantified_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != all(quantified_value))

      {:not, {:!=, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) != any(quantified_value)))

      {:!=, {:any, quantified_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != any(quantified_value))

      {:not, {:!=, nil}} ->
        dynamic([_, _, _, _, q], is_nil(field(q, ^key)))

      {:!=, nil} ->
        dynamic([_, _, _, _, q], not is_nil(field(q, ^key)))

      {:not, {:!=, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:!=, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:!=, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:!=, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:!=, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:!=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:!=, value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^value)

      {:!=, value} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^value)

      {:not, {:ne, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) != all(quantified_value)))

      {:ne, {:all, quantified_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != all(quantified_value))

      {:not, {:ne, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, q], not (field(q, ^key) != any(quantified_value)))

      {:ne, {:any, quantified_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != any(quantified_value))

      {:not, {:ne, nil}} ->
        dynamic([_, _, _, _, q], is_nil(field(q, ^key)))

      {:ne, nil} ->
        dynamic([_, _, _, _, q], not is_nil(field(q, ^key)))

      {:not, {:ne, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:ne, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:ne, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:ne, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:ne, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:ne, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:ne, value}} ->
        dynamic([_, _, _, _, q], field(q, ^key) == ^value)

      {:ne, value} ->
        dynamic([_, _, _, _, q], field(q, ^key) != ^value)

      {:not, {:avg, {:>, value}}} ->
        dynamic([_, _, _, _, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:>, value}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:>=, value}}} ->
        dynamic([_, _, _, _, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:>=, value}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:<, value}}} ->
        dynamic([_, _, _, _, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:<, value}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:<=, value}}} ->
        dynamic([_, _, _, _, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:<=, value}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:gt, value}}} ->
        dynamic([_, _, _, _, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:gt, value}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:gte, value}}} ->
        dynamic([_, _, _, _, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:gte, value}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:lt, value}}} ->
        dynamic([_, _, _, _, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:lt, value}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:lte, value}}} ->
        dynamic([_, _, _, _, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:lte, value}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:==, nil}}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:==, nil}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:==, value}}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:avg, {:==, value}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:eq, nil}}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:eq, nil}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:eq, value}}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:avg, {:eq, value}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:!=, nil}}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:!=, nil}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:!=, value}}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:avg, {:!=, value}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:not, {:avg, {:ne, nil}}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:ne, nil}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:ne, value}}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:avg, {:ne, value}} ->
        dynamic([_, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:not, {:count, {:>, value}}} ->
        dynamic([_, _, _, _, q], not (count(field(q, ^key)) > ^value))

      {:count, {:>, value}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:>=, value}}} ->
        dynamic([_, _, _, _, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:>=, value}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:<, value}}} ->
        dynamic([_, _, _, _, q], not (count(field(q, ^key)) < ^value))

      {:count, {:<, value}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:<=, value}}} ->
        dynamic([_, _, _, _, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:<=, value}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:gt, value}}} ->
        dynamic([_, _, _, _, q], not (count(field(q, ^key)) > ^value))

      {:count, {:gt, value}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:gte, value}}} ->
        dynamic([_, _, _, _, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:gte, value}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:lt, value}}} ->
        dynamic([_, _, _, _, q], not (count(field(q, ^key)) < ^value))

      {:count, {:lt, value}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:lte, value}}} ->
        dynamic([_, _, _, _, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:lte, value}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:==, nil}}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:count, {:==, nil}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:==, value}}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) != ^value)

      {:count, {:==, value}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:eq, nil}}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:count, {:eq, nil}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:eq, value}}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) != ^value)

      {:count, {:eq, value}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:!=, nil}}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:count, {:!=, nil}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:!=, value}}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) == ^value)

      {:count, {:!=, value}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) != ^value)

      {:not, {:count, {:ne, nil}}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:count, {:ne, nil}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:ne, value}}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) == ^value)

      {:count, {:ne, value}} ->
        dynamic([_, _, _, _, q], count(field(q, ^key)) != ^value)

      {:not, {:max, {:>, value}}} ->
        dynamic([_, _, _, _, q], not (max(field(q, ^key)) > ^value))

      {:max, {:>, value}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:>=, value}}} ->
        dynamic([_, _, _, _, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:>=, value}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:<, value}}} ->
        dynamic([_, _, _, _, q], not (max(field(q, ^key)) < ^value))

      {:max, {:<, value}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:<=, value}}} ->
        dynamic([_, _, _, _, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:<=, value}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:gt, value}}} ->
        dynamic([_, _, _, _, q], not (max(field(q, ^key)) > ^value))

      {:max, {:gt, value}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:gte, value}}} ->
        dynamic([_, _, _, _, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:gte, value}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:lt, value}}} ->
        dynamic([_, _, _, _, q], not (max(field(q, ^key)) < ^value))

      {:max, {:lt, value}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:lte, value}}} ->
        dynamic([_, _, _, _, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:lte, value}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:==, nil}}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:max, {:==, nil}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:==, value}}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) != ^value)

      {:max, {:==, value}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:eq, nil}}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:max, {:eq, nil}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:eq, value}}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) != ^value)

      {:max, {:eq, value}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:!=, nil}}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:max, {:!=, nil}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:!=, value}}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) == ^value)

      {:max, {:!=, value}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) != ^value)

      {:not, {:max, {:ne, nil}}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:max, {:ne, nil}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:ne, value}}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) == ^value)

      {:max, {:ne, value}} ->
        dynamic([_, _, _, _, q], max(field(q, ^key)) != ^value)

      {:not, {:min, {:>, value}}} ->
        dynamic([_, _, _, _, q], not (min(field(q, ^key)) > ^value))

      {:min, {:>, value}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:>=, value}}} ->
        dynamic([_, _, _, _, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:>=, value}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:<, value}}} ->
        dynamic([_, _, _, _, q], not (min(field(q, ^key)) < ^value))

      {:min, {:<, value}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:<=, value}}} ->
        dynamic([_, _, _, _, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:<=, value}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:gt, value}}} ->
        dynamic([_, _, _, _, q], not (min(field(q, ^key)) > ^value))

      {:min, {:gt, value}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:gte, value}}} ->
        dynamic([_, _, _, _, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:gte, value}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:lt, value}}} ->
        dynamic([_, _, _, _, q], not (min(field(q, ^key)) < ^value))

      {:min, {:lt, value}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:lte, value}}} ->
        dynamic([_, _, _, _, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:lte, value}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:==, nil}}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:min, {:==, nil}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:==, value}}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) != ^value)

      {:min, {:==, value}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:eq, nil}}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:min, {:eq, nil}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:eq, value}}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) != ^value)

      {:min, {:eq, value}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:!=, nil}}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:min, {:!=, nil}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:!=, value}}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) == ^value)

      {:min, {:!=, value}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) != ^value)

      {:not, {:min, {:ne, nil}}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:min, {:ne, nil}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:ne, value}}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) == ^value)

      {:min, {:ne, value}} ->
        dynamic([_, _, _, _, q], min(field(q, ^key)) != ^value)

      {:not, {:sum, {:>, value}}} ->
        dynamic([_, _, _, _, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:>, value}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:>=, value}}} ->
        dynamic([_, _, _, _, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:>=, value}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:<, value}}} ->
        dynamic([_, _, _, _, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:<, value}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:<=, value}}} ->
        dynamic([_, _, _, _, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:<=, value}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:gt, value}}} ->
        dynamic([_, _, _, _, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:gt, value}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:gte, value}}} ->
        dynamic([_, _, _, _, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:gte, value}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:lt, value}}} ->
        dynamic([_, _, _, _, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:lt, value}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:lte, value}}} ->
        dynamic([_, _, _, _, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:lte, value}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:==, nil}}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:==, nil}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:==, value}}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:sum, {:==, value}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:eq, nil}}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:eq, nil}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:eq, value}}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:sum, {:eq, value}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:!=, nil}}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:!=, nil}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:!=, value}}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:sum, {:!=, value}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:not, {:sum, {:ne, nil}}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:ne, nil}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:ne, value}}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:sum, {:ne, value}} ->
        dynamic([_, _, _, _, q], sum(field(q, ^key)) != ^value)
    end
  end

  def dynamic_expr({:at, 6}, key, negated, value) do
    term =
      case negated do
        :not -> {:not, value}
        _ -> value
      end

    case term do
      {:not, {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:>, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ^wrapped_value))

      {:>, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ^wrapped_value)

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>, value}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ^value))

      {:>, value} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ^value)

      {:not, {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:>=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ^wrapped_value))

      {:>=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>=, value}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ^value))

      {:>=, value} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ^value)

      {:not, {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:<, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ^wrapped_value))

      {:<, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ^wrapped_value)

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<, value}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ^value))

      {:<, value} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ^value)

      {:not, {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:<=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ^wrapped_value))

      {:<=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<=, value}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ^value))

      {:<=, value} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ^value)

      {:not, {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:gt, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ^wrapped_value))

      {:gt, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ^wrapped_value)

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gt, value}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) > ^value))

      {:gt, value} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) > ^value)

      {:not, {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:gte, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ^wrapped_value))

      {:gte, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gte, value}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) >= ^value))

      {:gte, value} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) >= ^value)

      {:not, {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:lt, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ^wrapped_value))

      {:lt, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ^wrapped_value)

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lt, value}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) < ^value))

      {:lt, value} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) < ^value)

      {:not, {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:lte, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ^wrapped_value))

      {:lte, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lte, value}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) <= ^value))

      {:lte, value} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) <= ^value)

      {:not, {:==, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) == all(quantified_value)))

      {:==, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == all(quantified_value))

      {:not, {:==, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) == any(quantified_value)))

      {:==, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == any(quantified_value))

      {:not, {:==, nil}} ->
        dynamic([_, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:==, nil} ->
        dynamic([_, _, _, _, _, q], is_nil(field(q, ^key)))

      {:not, {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:not, {:==, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:==, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:==, value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ^value)

      {:==, value} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ^value)

      {:not, {:eq, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) == all(quantified_value)))

      {:eq, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == all(quantified_value))

      {:not, {:eq, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) == any(quantified_value)))

      {:eq, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == any(quantified_value))

      {:not, {:eq, nil}} ->
        dynamic([_, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:eq, nil} ->
        dynamic([_, _, _, _, _, q], is_nil(field(q, ^key)))

      {:not, {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:not, {:eq, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:eq, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:eq, value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ^value)

      {:eq, value} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ^value)

      {:not, {:!=, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) != all(quantified_value)))

      {:!=, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != all(quantified_value))

      {:not, {:!=, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) != any(quantified_value)))

      {:!=, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != any(quantified_value))

      {:not, {:!=, nil}} ->
        dynamic([_, _, _, _, _, q], is_nil(field(q, ^key)))

      {:!=, nil} ->
        dynamic([_, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:not, {:!=, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:!=, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:!=, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:!=, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:!=, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:!=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:!=, value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ^value)

      {:!=, value} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ^value)

      {:not, {:ne, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) != all(quantified_value)))

      {:ne, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != all(quantified_value))

      {:not, {:ne, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, q], not (field(q, ^key) != any(quantified_value)))

      {:ne, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != any(quantified_value))

      {:not, {:ne, nil}} ->
        dynamic([_, _, _, _, _, q], is_nil(field(q, ^key)))

      {:ne, nil} ->
        dynamic([_, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:not, {:ne, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:ne, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:ne, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:ne, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:ne, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:ne, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:ne, value}} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) == ^value)

      {:ne, value} ->
        dynamic([_, _, _, _, _, q], field(q, ^key) != ^value)

      {:not, {:avg, {:>, value}}} ->
        dynamic([_, _, _, _, _, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:>, value}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:>=, value}}} ->
        dynamic([_, _, _, _, _, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:>=, value}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:<, value}}} ->
        dynamic([_, _, _, _, _, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:<, value}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:<=, value}}} ->
        dynamic([_, _, _, _, _, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:<=, value}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:gt, value}}} ->
        dynamic([_, _, _, _, _, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:gt, value}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:gte, value}}} ->
        dynamic([_, _, _, _, _, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:gte, value}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:lt, value}}} ->
        dynamic([_, _, _, _, _, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:lt, value}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:lte, value}}} ->
        dynamic([_, _, _, _, _, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:lte, value}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:==, nil}}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:==, nil}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:==, value}}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:avg, {:==, value}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:eq, nil}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:eq, value}}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:avg, {:eq, value}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:!=, nil}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:!=, value}}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:avg, {:!=, value}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:not, {:avg, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:ne, nil}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:ne, value}}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:avg, {:ne, value}} ->
        dynamic([_, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:not, {:count, {:>, value}}} ->
        dynamic([_, _, _, _, _, q], not (count(field(q, ^key)) > ^value))

      {:count, {:>, value}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:>=, value}}} ->
        dynamic([_, _, _, _, _, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:>=, value}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:<, value}}} ->
        dynamic([_, _, _, _, _, q], not (count(field(q, ^key)) < ^value))

      {:count, {:<, value}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:<=, value}}} ->
        dynamic([_, _, _, _, _, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:<=, value}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:gt, value}}} ->
        dynamic([_, _, _, _, _, q], not (count(field(q, ^key)) > ^value))

      {:count, {:gt, value}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:gte, value}}} ->
        dynamic([_, _, _, _, _, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:gte, value}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:lt, value}}} ->
        dynamic([_, _, _, _, _, q], not (count(field(q, ^key)) < ^value))

      {:count, {:lt, value}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:lte, value}}} ->
        dynamic([_, _, _, _, _, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:lte, value}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:==, nil}}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:count, {:==, nil}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:==, value}}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:count, {:==, value}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:count, {:eq, nil}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:eq, value}}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:count, {:eq, value}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:count, {:!=, nil}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:!=, value}}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:count, {:!=, value}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:not, {:count, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:count, {:ne, nil}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:ne, value}}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:count, {:ne, value}} ->
        dynamic([_, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:not, {:max, {:>, value}}} ->
        dynamic([_, _, _, _, _, q], not (max(field(q, ^key)) > ^value))

      {:max, {:>, value}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:>=, value}}} ->
        dynamic([_, _, _, _, _, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:>=, value}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:<, value}}} ->
        dynamic([_, _, _, _, _, q], not (max(field(q, ^key)) < ^value))

      {:max, {:<, value}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:<=, value}}} ->
        dynamic([_, _, _, _, _, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:<=, value}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:gt, value}}} ->
        dynamic([_, _, _, _, _, q], not (max(field(q, ^key)) > ^value))

      {:max, {:gt, value}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:gte, value}}} ->
        dynamic([_, _, _, _, _, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:gte, value}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:lt, value}}} ->
        dynamic([_, _, _, _, _, q], not (max(field(q, ^key)) < ^value))

      {:max, {:lt, value}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:lte, value}}} ->
        dynamic([_, _, _, _, _, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:lte, value}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:==, nil}}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:max, {:==, nil}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:==, value}}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:max, {:==, value}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:max, {:eq, nil}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:eq, value}}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:max, {:eq, value}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:max, {:!=, nil}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:!=, value}}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:max, {:!=, value}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:not, {:max, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:max, {:ne, nil}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:ne, value}}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:max, {:ne, value}} ->
        dynamic([_, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:not, {:min, {:>, value}}} ->
        dynamic([_, _, _, _, _, q], not (min(field(q, ^key)) > ^value))

      {:min, {:>, value}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:>=, value}}} ->
        dynamic([_, _, _, _, _, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:>=, value}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:<, value}}} ->
        dynamic([_, _, _, _, _, q], not (min(field(q, ^key)) < ^value))

      {:min, {:<, value}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:<=, value}}} ->
        dynamic([_, _, _, _, _, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:<=, value}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:gt, value}}} ->
        dynamic([_, _, _, _, _, q], not (min(field(q, ^key)) > ^value))

      {:min, {:gt, value}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:gte, value}}} ->
        dynamic([_, _, _, _, _, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:gte, value}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:lt, value}}} ->
        dynamic([_, _, _, _, _, q], not (min(field(q, ^key)) < ^value))

      {:min, {:lt, value}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:lte, value}}} ->
        dynamic([_, _, _, _, _, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:lte, value}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:==, nil}}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:min, {:==, nil}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:==, value}}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:min, {:==, value}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:min, {:eq, nil}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:eq, value}}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:min, {:eq, value}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:min, {:!=, nil}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:!=, value}}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:min, {:!=, value}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:not, {:min, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:min, {:ne, nil}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:ne, value}}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:min, {:ne, value}} ->
        dynamic([_, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:not, {:sum, {:>, value}}} ->
        dynamic([_, _, _, _, _, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:>, value}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:>=, value}}} ->
        dynamic([_, _, _, _, _, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:>=, value}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:<, value}}} ->
        dynamic([_, _, _, _, _, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:<, value}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:<=, value}}} ->
        dynamic([_, _, _, _, _, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:<=, value}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:gt, value}}} ->
        dynamic([_, _, _, _, _, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:gt, value}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:gte, value}}} ->
        dynamic([_, _, _, _, _, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:gte, value}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:lt, value}}} ->
        dynamic([_, _, _, _, _, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:lt, value}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:lte, value}}} ->
        dynamic([_, _, _, _, _, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:lte, value}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:==, nil}}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:==, nil}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:==, value}}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:sum, {:==, value}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:eq, nil}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:eq, value}}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:sum, {:eq, value}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:!=, nil}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:!=, value}}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:sum, {:!=, value}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:not, {:sum, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:ne, nil}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:ne, value}}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:sum, {:ne, value}} ->
        dynamic([_, _, _, _, _, q], sum(field(q, ^key)) != ^value)
    end
  end

  def dynamic_expr({:at, 7}, key, negated, value) do
    term =
      case negated do
        :not -> {:not, value}
        _ -> value
      end

    case term do
      {:not, {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:>, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ^wrapped_value))

      {:>, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ^wrapped_value)

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>, value}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ^value))

      {:>, value} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ^value)

      {:not, {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:>=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ^wrapped_value))

      {:>=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ^value))

      {:>=, value} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ^value)

      {:not, {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:<, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ^wrapped_value))

      {:<, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ^wrapped_value)

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<, value}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ^value))

      {:<, value} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ^value)

      {:not, {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:<=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ^wrapped_value))

      {:<=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ^value))

      {:<=, value} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ^value)

      {:not, {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:gt, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ^wrapped_value))

      {:gt, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ^wrapped_value)

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) > ^value))

      {:gt, value} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) > ^value)

      {:not, {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:gte, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ^wrapped_value))

      {:gte, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) >= ^value))

      {:gte, value} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) >= ^value)

      {:not, {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:lt, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ^wrapped_value))

      {:lt, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ^wrapped_value)

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) < ^value))

      {:lt, value} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) < ^value)

      {:not, {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:lte, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ^wrapped_value))

      {:lte, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) <= ^value))

      {:lte, value} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) <= ^value)

      {:not, {:==, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) == all(quantified_value)))

      {:==, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == all(quantified_value))

      {:not, {:==, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) == any(quantified_value)))

      {:==, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == any(quantified_value))

      {:not, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:==, nil} ->
        dynamic([_, _, _, _, _, _, q], is_nil(field(q, ^key)))

      {:not, {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:not, {:==, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:==, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:==, value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ^value)

      {:==, value} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ^value)

      {:not, {:eq, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) == all(quantified_value)))

      {:eq, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == all(quantified_value))

      {:not, {:eq, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) == any(quantified_value)))

      {:eq, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == any(quantified_value))

      {:not, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:eq, nil} ->
        dynamic([_, _, _, _, _, _, q], is_nil(field(q, ^key)))

      {:not, {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:not, {:eq, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:eq, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ^value)

      {:eq, value} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ^value)

      {:not, {:!=, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) != all(quantified_value)))

      {:!=, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != all(quantified_value))

      {:not, {:!=, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) != any(quantified_value)))

      {:!=, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != any(quantified_value))

      {:not, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, q], is_nil(field(q, ^key)))

      {:!=, nil} ->
        dynamic([_, _, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:not, {:!=, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:!=, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:!=, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:!=, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:!=, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:!=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ^value)

      {:!=, value} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ^value)

      {:not, {:ne, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) != all(quantified_value)))

      {:ne, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != all(quantified_value))

      {:not, {:ne, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, q], not (field(q, ^key) != any(quantified_value)))

      {:ne, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != any(quantified_value))

      {:not, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, q], is_nil(field(q, ^key)))

      {:ne, nil} ->
        dynamic([_, _, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:not, {:ne, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:ne, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:ne, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:ne, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:ne, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:ne, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) == ^value)

      {:ne, value} ->
        dynamic([_, _, _, _, _, _, q], field(q, ^key) != ^value)

      {:not, {:avg, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:>, value}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:<, value}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:avg, {:==, value}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:avg, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:avg, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:not, {:avg, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:avg, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:not, {:count, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (count(field(q, ^key)) > ^value))

      {:count, {:>, value}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (count(field(q, ^key)) < ^value))

      {:count, {:<, value}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (count(field(q, ^key)) > ^value))

      {:count, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (count(field(q, ^key)) < ^value))

      {:count, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:count, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:count, {:==, value}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:count, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:count, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:count, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:count, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:not, {:count, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:count, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:count, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:not, {:max, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (max(field(q, ^key)) > ^value))

      {:max, {:>, value}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (max(field(q, ^key)) < ^value))

      {:max, {:<, value}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (max(field(q, ^key)) > ^value))

      {:max, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (max(field(q, ^key)) < ^value))

      {:max, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:max, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:max, {:==, value}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:max, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:max, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:max, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:max, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:not, {:max, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:max, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:max, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:not, {:min, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (min(field(q, ^key)) > ^value))

      {:min, {:>, value}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (min(field(q, ^key)) < ^value))

      {:min, {:<, value}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (min(field(q, ^key)) > ^value))

      {:min, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (min(field(q, ^key)) < ^value))

      {:min, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:min, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:min, {:==, value}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:min, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:min, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:min, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:min, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:not, {:min, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:min, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:min, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:not, {:sum, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:>, value}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:<, value}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:sum, {:==, value}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:sum, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:sum, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:not, {:sum, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:sum, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, q], sum(field(q, ^key)) != ^value)
    end
  end

  def dynamic_expr({:at, 8}, key, negated, value) do
    term =
      case negated do
        :not -> {:not, value}
        _ -> value
      end

    case term do
      {:not, {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:>, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ^wrapped_value))

      {:>, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ^wrapped_value)

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ^value))

      {:>, value} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ^value)

      {:not, {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:>=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ^wrapped_value))

      {:>=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ^value))

      {:>=, value} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ^value)

      {:not, {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:<, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ^wrapped_value))

      {:<, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ^wrapped_value)

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ^value))

      {:<, value} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ^value)

      {:not, {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:<=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ^wrapped_value))

      {:<=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ^value))

      {:<=, value} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ^value)

      {:not, {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:gt, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ^wrapped_value))

      {:gt, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ^wrapped_value)

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) > ^value))

      {:gt, value} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) > ^value)

      {:not, {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:gte, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ^wrapped_value))

      {:gte, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) >= ^value))

      {:gte, value} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) >= ^value)

      {:not, {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:lt, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ^wrapped_value))

      {:lt, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ^wrapped_value)

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) < ^value))

      {:lt, value} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) < ^value)

      {:not, {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:lte, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ^wrapped_value))

      {:lte, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) <= ^value))

      {:lte, value} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) <= ^value)

      {:not, {:==, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) == all(quantified_value)))

      {:==, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == all(quantified_value))

      {:not, {:==, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) == any(quantified_value)))

      {:==, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == any(quantified_value))

      {:not, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:==, nil} ->
        dynamic([_, _, _, _, _, _, _, q], is_nil(field(q, ^key)))

      {:not, {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:not, {:==, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:==, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ^value)

      {:==, value} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ^value)

      {:not, {:eq, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) == all(quantified_value)))

      {:eq, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == all(quantified_value))

      {:not, {:eq, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) == any(quantified_value)))

      {:eq, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == any(quantified_value))

      {:not, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:eq, nil} ->
        dynamic([_, _, _, _, _, _, _, q], is_nil(field(q, ^key)))

      {:not, {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:not, {:eq, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:eq, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ^value)

      {:eq, value} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ^value)

      {:not, {:!=, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) != all(quantified_value)))

      {:!=, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != all(quantified_value))

      {:not, {:!=, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) != any(quantified_value)))

      {:!=, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != any(quantified_value))

      {:not, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], is_nil(field(q, ^key)))

      {:!=, nil} ->
        dynamic([_, _, _, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:not, {:!=, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:!=, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:!=, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:!=, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:!=, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:!=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ^value)

      {:!=, value} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ^value)

      {:not, {:ne, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) != all(quantified_value)))

      {:ne, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != all(quantified_value))

      {:not, {:ne, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (field(q, ^key) != any(quantified_value)))

      {:ne, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != any(quantified_value))

      {:not, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], is_nil(field(q, ^key)))

      {:ne, nil} ->
        dynamic([_, _, _, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:not, {:ne, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:ne, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:ne, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:ne, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:ne, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:ne, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) == ^value)

      {:ne, value} ->
        dynamic([_, _, _, _, _, _, _, q], field(q, ^key) != ^value)

      {:not, {:avg, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:avg, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:avg, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:avg, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:not, {:avg, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:avg, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:not, {:count, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (count(field(q, ^key)) > ^value))

      {:count, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (count(field(q, ^key)) < ^value))

      {:count, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (count(field(q, ^key)) > ^value))

      {:count, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (count(field(q, ^key)) < ^value))

      {:count, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:count, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:count, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:count, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:count, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:count, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:count, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:not, {:count, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:count, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:count, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:not, {:max, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (max(field(q, ^key)) > ^value))

      {:max, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (max(field(q, ^key)) < ^value))

      {:max, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (max(field(q, ^key)) > ^value))

      {:max, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (max(field(q, ^key)) < ^value))

      {:max, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:max, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:max, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:max, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:max, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:max, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:max, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:not, {:max, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:max, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:max, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:not, {:min, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (min(field(q, ^key)) > ^value))

      {:min, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (min(field(q, ^key)) < ^value))

      {:min, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (min(field(q, ^key)) > ^value))

      {:min, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (min(field(q, ^key)) < ^value))

      {:min, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:min, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:min, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:min, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:min, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:min, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:min, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:not, {:min, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:min, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:min, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:not, {:sum, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:sum, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:sum, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:sum, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:not, {:sum, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:sum, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^value)
    end
  end

  def dynamic_expr({:at, 9}, key, negated, value) do
    term =
      case negated do
        :not -> {:not, value}
        _ -> value
      end

    case term do
      {:not, {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:>, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ^wrapped_value))

      {:>, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ^wrapped_value)

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ^value))

      {:>, value} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ^value)

      {:not, {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:>=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ^wrapped_value))

      {:>=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ^value))

      {:>=, value} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ^value)

      {:not, {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:<, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ^wrapped_value))

      {:<, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ^wrapped_value)

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ^value))

      {:<, value} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ^value)

      {:not, {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:<=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ^wrapped_value))

      {:<=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ^value))

      {:<=, value} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ^value)

      {:not, {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:gt, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ^wrapped_value))

      {:gt, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ^wrapped_value)

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) > ^value))

      {:gt, value} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) > ^value)

      {:not, {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:gte, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ^wrapped_value))

      {:gte, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ^value))

      {:gte, value} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) >= ^value)

      {:not, {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:lt, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ^wrapped_value))

      {:lt, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ^wrapped_value)

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) < ^value))

      {:lt, value} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) < ^value)

      {:not, {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:lte, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ^wrapped_value))

      {:lte, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ^value))

      {:lte, value} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) <= ^value)

      {:not, {:==, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) == all(quantified_value)))

      {:==, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == all(quantified_value))

      {:not, {:==, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) == any(quantified_value)))

      {:==, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == any(quantified_value))

      {:not, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:==, nil} ->
        dynamic([_, _, _, _, _, _, _, _, q], is_nil(field(q, ^key)))

      {:not, {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:not, {:==, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:==, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ^value)

      {:==, value} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ^value)

      {:not, {:eq, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) == all(quantified_value)))

      {:eq, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == all(quantified_value))

      {:not, {:eq, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) == any(quantified_value)))

      {:eq, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == any(quantified_value))

      {:not, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:eq, nil} ->
        dynamic([_, _, _, _, _, _, _, _, q], is_nil(field(q, ^key)))

      {:not, {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:not, {:eq, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:eq, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ^value)

      {:eq, value} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ^value)

      {:not, {:!=, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) != all(quantified_value)))

      {:!=, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != all(quantified_value))

      {:not, {:!=, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) != any(quantified_value)))

      {:!=, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != any(quantified_value))

      {:not, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], is_nil(field(q, ^key)))

      {:!=, nil} ->
        dynamic([_, _, _, _, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:not, {:!=, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:!=, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:!=, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:!=, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:!=, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:!=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:!=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ^value)

      {:!=, value} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ^value)

      {:not, {:ne, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) != all(quantified_value)))

      {:ne, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != all(quantified_value))

      {:not, {:ne, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (field(q, ^key) != any(quantified_value)))

      {:ne, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != any(quantified_value))

      {:not, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], is_nil(field(q, ^key)))

      {:ne, nil} ->
        dynamic([_, _, _, _, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:not, {:ne, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ^{:+, field: field_name, value: scalar_value})

      {:ne, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ^{:+, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ^{:-, field: field_name, value: scalar_value})

      {:ne, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ^{:-, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ^{:*, field: field_name, value: scalar_value})

      {:ne, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ^{:*, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ^{:/, field: field_name, value: scalar_value})

      {:ne, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ^{:/, field: field_name, value: scalar_value})

      {:not, {:ne, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:ne, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) == ^value)

      {:ne, value} ->
        dynamic([_, _, _, _, _, _, _, _, q], field(q, ^key) != ^value)

      {:not, {:avg, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:avg, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:avg, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:avg, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:not, {:avg, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:avg, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:not, {:count, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (count(field(q, ^key)) > ^value))

      {:count, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (count(field(q, ^key)) < ^value))

      {:count, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (count(field(q, ^key)) > ^value))

      {:count, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (count(field(q, ^key)) < ^value))

      {:count, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:count, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:count, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:count, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:count, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:count, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:count, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:not, {:count, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:count, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:count, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:not, {:max, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (max(field(q, ^key)) > ^value))

      {:max, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (max(field(q, ^key)) < ^value))

      {:max, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (max(field(q, ^key)) > ^value))

      {:max, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (max(field(q, ^key)) < ^value))

      {:max, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:max, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:max, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:max, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:max, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:max, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:max, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:not, {:max, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:max, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:max, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:not, {:min, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (min(field(q, ^key)) > ^value))

      {:min, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (min(field(q, ^key)) < ^value))

      {:min, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (min(field(q, ^key)) > ^value))

      {:min, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (min(field(q, ^key)) < ^value))

      {:min, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:min, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:min, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:min, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:min, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:min, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:min, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:not, {:min, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:min, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:min, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:not, {:sum, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:sum, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:sum, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:sum, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:not, {:sum, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:sum, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^value)
    end
  end

  def dynamic_expr({:at, 10}, key, negated, value) do
    term =
      case negated do
        :not -> {:not, value}
        _ -> value
      end

    case term do
      {:not, {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:>, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:>, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:>, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:>, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:>, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ^wrapped_value))

      {:>, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ^wrapped_value)

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > from_now(^datetime_count, "millisecond"))
        )

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:>, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > from_now(^datetime_count, "microsecond"))
        )

      {:>, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ^value))

      {:>, value} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ^value)

      {:not, {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:>=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:>=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:>=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:>=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:>=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ^wrapped_value))

      {:>=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= from_now(^datetime_count, "millisecond"))
        )

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:>=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:>=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= from_now(^datetime_count, "microsecond"))
        )

      {:>=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:>=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ^value))

      {:>=, value} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ^value)

      {:not, {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:<, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:<, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:<, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:<, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:<, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ^wrapped_value))

      {:<, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ^wrapped_value)

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < from_now(^datetime_count, "millisecond"))
        )

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:<, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < from_now(^datetime_count, "microsecond"))
        )

      {:<, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ^value))

      {:<, value} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ^value)

      {:not, {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:<=, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:<=, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:<=, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:<=, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:<=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ^wrapped_value))

      {:<=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= from_now(^datetime_count, "millisecond"))
        )

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:<=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:<=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= from_now(^datetime_count, "microsecond"))
        )

      {:<=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:<=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ^value))

      {:<=, value} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ^value)

      {:not, {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) + ^scalar_value))

      {:gt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) + ^scalar_value)

      {:not, {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) - ^scalar_value))

      {:gt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) - ^scalar_value)

      {:not, {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) * ^scalar_value))

      {:gt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) * ^scalar_value)

      {:not, {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > field(q, ^field_name) / ^scalar_value))

      {:gt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > field(q, ^field_name) / ^scalar_value)

      {:not, {:gt, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ^wrapped_value))

      {:gt, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ^wrapped_value)

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "year")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "year"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "year")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "year"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "month")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "month"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "month")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "month"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "week")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "week"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "week")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "week"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "day")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "day"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "day")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "day"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "hour")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "hour"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "hour")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "hour"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "minute")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "minute"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "minute")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "minute"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "second")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "second"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > from_now(^datetime_count, "second")))

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "second"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "millisecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "millisecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > from_now(^datetime_count, "millisecond"))
        )

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "millisecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) > datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ago(^datetime_count, "microsecond")))

      {:gt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ago(^datetime_count, "microsecond"))

      {:not, {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) > from_now(^datetime_count, "microsecond"))
        )

      {:gt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > from_now(^datetime_count, "microsecond"))

      {:not, {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) > fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) > ^value))

      {:gt, value} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) > ^value)

      {:not, {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) + ^scalar_value))

      {:gte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) + ^scalar_value)

      {:not, {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) - ^scalar_value))

      {:gte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) - ^scalar_value)

      {:not, {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) * ^scalar_value))

      {:gte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) * ^scalar_value)

      {:not, {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= field(q, ^field_name) / ^scalar_value))

      {:gte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= field(q, ^field_name) / ^scalar_value)

      {:not, {:gte, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ^wrapped_value))

      {:gte, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ^wrapped_value)

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "year")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "year"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "year")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "year"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "month")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "month"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "month")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "month"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "week")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "week"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "week")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "week"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "day")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "day"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "day")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "day"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "hour")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "hour"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "hour")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "hour"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "minute")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "minute"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "minute")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "minute"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "second")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "second"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= from_now(^datetime_count, "second")))

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "second"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "millisecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "millisecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= from_now(^datetime_count, "millisecond"))
        )

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "millisecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:gte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) >= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ago(^datetime_count, "microsecond")))

      {:gte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ago(^datetime_count, "microsecond"))

      {:not, {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) >= from_now(^datetime_count, "microsecond"))
        )

      {:gte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= from_now(^datetime_count, "microsecond"))

      {:not, {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) >=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:gte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) >= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) >= ^value))

      {:gte, value} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) >= ^value)

      {:not, {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) + ^scalar_value))

      {:lt, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) + ^scalar_value)

      {:not, {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) - ^scalar_value))

      {:lt, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) - ^scalar_value)

      {:not, {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) * ^scalar_value))

      {:lt, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) * ^scalar_value)

      {:not, {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < field(q, ^field_name) / ^scalar_value))

      {:lt, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < field(q, ^field_name) / ^scalar_value)

      {:not, {:lt, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ^wrapped_value))

      {:lt, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ^wrapped_value)

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "year")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "year"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "year")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "year"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "month")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "month"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "month")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "month"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "week")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "week"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "week")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "week"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "day")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "day"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "day")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "day"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "hour")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "hour"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "hour")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "hour"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "minute")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "minute"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "minute")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "minute"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "second")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "second"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < from_now(^datetime_count, "second")))

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "second"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "millisecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "millisecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < from_now(^datetime_count, "millisecond"))
        )

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "millisecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lt, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) < datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ago(^datetime_count, "microsecond")))

      {:lt, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ago(^datetime_count, "microsecond"))

      {:not, {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) < from_now(^datetime_count, "microsecond"))
        )

      {:lt, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < from_now(^datetime_count, "microsecond"))

      {:not, {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lt, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) < fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) < ^value))

      {:lt, value} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) < ^value)

      {:not, {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) + ^scalar_value))

      {:lte, {:value, {:+, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) + ^scalar_value)

      {:not, {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) - ^scalar_value))

      {:lte, {:value, {:-, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) - ^scalar_value)

      {:not, {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) * ^scalar_value))

      {:lte, {:value, {:*, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) * ^scalar_value)

      {:not, {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= field(q, ^field_name) / ^scalar_value))

      {:lte, {:value, {:/, {{:field, field_name}, {:value, scalar_value}}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= field(q, ^field_name) / ^scalar_value)

      {:not, {:lte, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ^wrapped_value))

      {:lte, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ^wrapped_value)

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "year")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "year"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "year")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "year"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "month")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "month"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "month")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "month"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "week")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "week"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "week")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "week"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "day")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "day"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "day")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "day"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "hour")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "hour"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "hour")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "hour"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "minute")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "minute"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "minute")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "minute"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "second")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "second"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= from_now(^datetime_count, "second")))

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "second"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "millisecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "millisecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= from_now(^datetime_count, "millisecond"))
        )

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "millisecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "millisecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:lte, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) <= datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ago(^datetime_count, "microsecond")))

      {:lte, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ago(^datetime_count, "microsecond"))

      {:not, {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (field(q, ^key) <= from_now(^datetime_count, "microsecond"))
        )

      {:lte, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= from_now(^datetime_count, "microsecond"))

      {:not, {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          not (fragment("date(?)", field(q, ^key)) <=
                 fragment("date(?)", from_now(^datetime_count, "microsecond")))
        )

      {:lte, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) <= fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) <= ^value))

      {:lte, value} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) <= ^value)

      {:not, {:==, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) == all(quantified_value)))

      {:==, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == all(quantified_value))

      {:not, {:==, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) == any(quantified_value)))

      {:==, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == any(quantified_value))

      {:not, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:==, nil} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], is_nil(field(q, ^key)))

      {:not, {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:==, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:not, {:==, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:==, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:==, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:==, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:==, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:==, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:==, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ^value)

      {:==, value} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ^value)

      {:not, {:eq, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) == all(quantified_value)))

      {:eq, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == all(quantified_value))

      {:not, {:eq, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) == any(quantified_value)))

      {:eq, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == any(quantified_value))

      {:not, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:eq, nil} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], is_nil(field(q, ^key)))

      {:not, {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:eq, {:value, {arithmetic_op, field: field_name, value: scalar_value}}}
      when arithmetic_op in [:+, :-, :*, :/] ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == ^{arithmetic_op, field: field_name, value: scalar_value}
        )

      {:not, {:eq, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:eq, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:eq, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:not, {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:eq, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:not, {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:eq, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ^value)

      {:eq, value} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ^value)

      {:not, {:!=, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) != all(quantified_value)))

      {:!=, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != all(quantified_value))

      {:not, {:!=, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) != any(quantified_value)))

      {:!=, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != any(quantified_value))

      {:not, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], is_nil(field(q, ^key)))

      {:!=, nil} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:not, {:!=, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == ^{:+, field: field_name, value: scalar_value}
        )

      {:!=, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != ^{:+, field: field_name, value: scalar_value}
        )

      {:not, {:!=, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == ^{:-, field: field_name, value: scalar_value}
        )

      {:!=, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != ^{:-, field: field_name, value: scalar_value}
        )

      {:not, {:!=, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == ^{:*, field: field_name, value: scalar_value}
        )

      {:!=, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != ^{:*, field: field_name, value: scalar_value}
        )

      {:not, {:!=, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == ^{:/, field: field_name, value: scalar_value}
        )

      {:!=, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != ^{:/, field: field_name, value: scalar_value}
        )

      {:not, {:!=, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:!=, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:!=, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:!=, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:!=, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ^value)

      {:!=, value} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ^value)

      {:not, {:ne, {:all, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) != all(quantified_value)))

      {:ne, {:all, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != all(quantified_value))

      {:not, {:ne, {:any, quantified_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (field(q, ^key) != any(quantified_value)))

      {:ne, {:any, quantified_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != any(quantified_value))

      {:not, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], is_nil(field(q, ^key)))

      {:ne, nil} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not is_nil(field(q, ^key)))

      {:not, {:ne, {:value, {:+, field: field_name, value: scalar_value}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == ^{:+, field: field_name, value: scalar_value}
        )

      {:ne, {:value, {:+, field: field_name, value: scalar_value}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != ^{:+, field: field_name, value: scalar_value}
        )

      {:not, {:ne, {:value, {:-, field: field_name, value: scalar_value}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == ^{:-, field: field_name, value: scalar_value}
        )

      {:ne, {:value, {:-, field: field_name, value: scalar_value}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != ^{:-, field: field_name, value: scalar_value}
        )

      {:not, {:ne, {:value, {:*, field: field_name, value: scalar_value}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == ^{:*, field: field_name, value: scalar_value}
        )

      {:ne, {:value, {:*, field: field_name, value: scalar_value}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != ^{:*, field: field_name, value: scalar_value}
        )

      {:not, {:ne, {:value, {:/, field: field_name, value: scalar_value}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == ^{:/, field: field_name, value: scalar_value}
        )

      {:ne, {:value, {:/, field: field_name, value: scalar_value}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != ^{:/, field: field_name, value: scalar_value}
        )

      {:not, {:ne, {:value, wrapped_value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ^wrapped_value)

      {:ne, {:value, wrapped_value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ^wrapped_value)

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "year")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "year"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "year"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "year"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "year"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "year"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "year"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "year"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "month")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "month"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "month"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "month"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "month"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "month"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "month"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "month"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "week")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "week"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "week"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "week"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "week"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "week"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "week"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "week"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "day")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "day"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "day"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "day"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "day"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "day"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "day"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "day"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "hour")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "hour"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "hour"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "hour"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "hour"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "hour"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "hour"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "hour"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "minute")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "minute"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "minute"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "minute"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "minute"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "minute"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "minute"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "minute"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "second")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "second"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "second"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "second"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "second"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "second"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "second"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "second"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "millisecond")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "millisecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "millisecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "millisecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "millisecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "millisecond"))
        )

      {:not, {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) == datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:ne, {:datetime, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          field(q, ^key) != datetime_add(field(q, ^field_name), ^datetime_count, "microsecond")
        )

      {:not, {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ago(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ago(^datetime_count, "microsecond"))

      {:not, {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == from_now(^datetime_count, "microsecond"))

      {:ne, {:datetime, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != from_now(^datetime_count, "microsecond"))

      {:not, {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) ==
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:add, field: field_name, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) !=
            fragment("date(?)", datetime_add(field(q, ^field_name), ^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:ago, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", ago(^datetime_count, "microsecond"))
        )

      {:not, {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) == fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:ne, {:date, {:from_now, count: datetime_count, interval: "microsecond"}}} ->
        dynamic(
          [_, _, _, _, _, _, _, _, _, q],
          fragment("date(?)", field(q, ^key)) != fragment("date(?)", from_now(^datetime_count, "microsecond"))
        )

      {:not, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) == ^value)

      {:ne, value} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], field(q, ^key) != ^value)

      {:not, {:avg, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (avg(field(q, ^key)) > ^value))

      {:avg, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) > ^value)

      {:not, {:avg, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (avg(field(q, ^key)) >= ^value))

      {:avg, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) >= ^value)

      {:not, {:avg, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (avg(field(q, ^key)) < ^value))

      {:avg, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) < ^value)

      {:not, {:avg, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (avg(field(q, ^key)) <= ^value))

      {:avg, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) <= ^value)

      {:not, {:avg, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:avg, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:avg, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:not, {:avg, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:avg, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:not, {:avg, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:avg, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:not, {:avg, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^nil)

      {:avg, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^nil)

      {:not, {:avg, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) == ^value)

      {:avg, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], avg(field(q, ^key)) != ^value)

      {:not, {:count, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (count(field(q, ^key)) > ^value))

      {:count, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (count(field(q, ^key)) < ^value))

      {:count, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (count(field(q, ^key)) > ^value))

      {:count, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) > ^value)

      {:not, {:count, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (count(field(q, ^key)) >= ^value))

      {:count, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) >= ^value)

      {:not, {:count, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (count(field(q, ^key)) < ^value))

      {:count, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) < ^value)

      {:not, {:count, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (count(field(q, ^key)) <= ^value))

      {:count, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) <= ^value)

      {:not, {:count, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:count, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:count, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:count, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:not, {:count, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:count, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:not, {:count, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:count, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:count, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:not, {:count, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) == ^nil)

      {:count, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) != ^nil)

      {:not, {:count, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) == ^value)

      {:count, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], count(field(q, ^key)) != ^value)

      {:not, {:max, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (max(field(q, ^key)) > ^value))

      {:max, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (max(field(q, ^key)) < ^value))

      {:max, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (max(field(q, ^key)) > ^value))

      {:max, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) > ^value)

      {:not, {:max, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (max(field(q, ^key)) >= ^value))

      {:max, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) >= ^value)

      {:not, {:max, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (max(field(q, ^key)) < ^value))

      {:max, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) < ^value)

      {:not, {:max, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (max(field(q, ^key)) <= ^value))

      {:max, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) <= ^value)

      {:not, {:max, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:max, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:max, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:max, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:not, {:max, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:max, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:not, {:max, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:max, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:max, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:not, {:max, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) == ^nil)

      {:max, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) != ^nil)

      {:not, {:max, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) == ^value)

      {:max, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], max(field(q, ^key)) != ^value)

      {:not, {:min, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (min(field(q, ^key)) > ^value))

      {:min, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (min(field(q, ^key)) < ^value))

      {:min, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (min(field(q, ^key)) > ^value))

      {:min, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) > ^value)

      {:not, {:min, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (min(field(q, ^key)) >= ^value))

      {:min, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) >= ^value)

      {:not, {:min, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (min(field(q, ^key)) < ^value))

      {:min, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) < ^value)

      {:not, {:min, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (min(field(q, ^key)) <= ^value))

      {:min, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) <= ^value)

      {:not, {:min, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:min, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:min, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:min, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:not, {:min, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:min, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:not, {:min, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:min, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:min, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:not, {:min, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) == ^nil)

      {:min, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) != ^nil)

      {:not, {:min, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) == ^value)

      {:min, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], min(field(q, ^key)) != ^value)

      {:not, {:sum, {:>, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:>, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:>=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:>=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:<, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:<, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:<=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:<=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:gt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (sum(field(q, ^key)) > ^value))

      {:sum, {:gt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) > ^value)

      {:not, {:sum, {:gte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (sum(field(q, ^key)) >= ^value))

      {:sum, {:gte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) >= ^value)

      {:not, {:sum, {:lt, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (sum(field(q, ^key)) < ^value))

      {:sum, {:lt, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) < ^value)

      {:not, {:sum, {:lte, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], not (sum(field(q, ^key)) <= ^value))

      {:sum, {:lte, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) <= ^value)

      {:not, {:sum, {:==, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:==, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:==, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:sum, {:==, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:eq, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:sum, {:eq, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:not, {:sum, {:eq, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:sum, {:eq, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:not, {:sum, {:!=, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:!=, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:!=, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:sum, {:!=, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^value)

      {:not, {:sum, {:ne, nil}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^nil)

      {:sum, {:ne, nil}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^nil)

      {:not, {:sum, {:ne, value}}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) == ^value)

      {:sum, {:ne, value}} ->
        dynamic([_, _, _, _, _, _, _, _, _, q], sum(field(q, ^key)) != ^value)
    end
  end

  def dynamic_expr(_, _, _, _) do
    nil
  end
end