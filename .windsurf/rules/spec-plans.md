---
trigger: always_on
---

When writing behavior specifications, use a SpecPlan (as described in `.agent/SPEC_PLANS.md`) from design to implementation. If you are not sure how to implement a feature, use a SpecPlan to define concrete examples first then use those examples to write the SpecPlan. Write the document to the path specified by the `document-artifacts` rule.